# Task List — Drone Q-Learning Simulation
**Version:** 1.00  
**Author:** AVISHAGL  
**Course:** RL-L02 — Reinforcement Learning, Bar-Ilan University  
**Instructor:** Dr. Yoram Segal  
**Date:** 2026-04-14  
**PRD Reference:** [docs/PRD.md](PRD.md)  
**Plan Reference:** [docs/PLAN.md](PLAN.md)

---

## Legend

| Symbol | Meaning |
|---|---|
| `[ ]` | Not started |
| `[~]` | In progress |
| `[x]` | Done |
| `P1` | Priority 1 — blocker |
| `P2` | Priority 2 — high |
| `P3` | Priority 3 — normal |

---

## Phase 1 — Project Bootstrap

### 1.1 Repository & Project Init

- [x] P1 Create project root directory `ex1/`
- [x] P1 Run `uv init` inside `ex1/`
- [x] P1 Verify `pyproject.toml` was generated by `uv init`
- [x] P1 Set `name = "drone-q-learning"` in `pyproject.toml`
- [x] P1 Set `version = "1.00"` in `pyproject.toml`
- [x] P1 Set `requires-python = ">=3.11"` in `pyproject.toml`
- [x] P1 Add `description` field to `pyproject.toml`
- [x] P1 Add `authors` field with student name to `pyproject.toml`
- [x] P1 Verify `uv.lock` file is created after `uv sync`
- [x] P1 Create `.gitignore` with standard Python entries
- [x] P1 Add `.env` to `.gitignore`
- [x] P1 Add `__pycache__/` to `.gitignore`
- [x] P1 Add `*.pyc` to `.gitignore`
- [x] P1 Add `.pytest_cache/` to `.gitignore`
- [x] P1 Add `.coverage` to `.gitignore`
- [x] P1 Add `htmlcov/` to `.gitignore`
- [x] P1 Add `dist/` to `.gitignore`
- [x] P1 Add `*.egg-info/` to `.gitignore`
- [x] P1 Create `.env-example` with placeholder keys
- [x] P1 Confirm `.env-example` is NOT in `.gitignore` (must be committed)
- [x] P1 Create empty `.env` for local use

### 1.2 Directory Skeleton

- [x] P1 Create `src/` directory
- [x] P1 Create `src/core/` directory
- [x] P1 Create `src/sdk/` directory
- [x] P1 Create `src/gui/` directory
- [x] P1 Create `src/core/__init__.py`
- [x] P1 Create `src/sdk/__init__.py`
- [x] P1 Create `src/gui/__init__.py`
- [x] P1 Create `src/main.py` (entry point, initially empty `pass`)
- [x] P1 Create `tests/` directory
- [x] P1 Create `tests/core/` directory
- [x] P1 Create `tests/sdk/` directory
- [x] P1 Create `tests/__init__.py`
- [x] P1 Create `tests/core/__init__.py`
- [x] P1 Create `tests/sdk/__init__.py`
- [x] P1 Create `tests/conftest.py`
- [x] P1 Create `config/` directory
- [x] P1 Create `config/env.json` with placeholder structure
- [x] P1 Create `config/rl.json` with placeholder structure
- [x] P1 Create `docs/` directory (already done via PRD/PLAN)
- [x] P1 Create `assets/` directory
- [x] P1 Create `assets/.gitkeep` to track empty directory

### 1.3 Dependency Management

- [x] P1 Add `numpy>=1.26` via `uv add numpy`
- [x] P1 Add `matplotlib>=3.8` via `uv add matplotlib`
- [x] P1 Add `pytest>=8.0` to dev group: `uv add --dev pytest`
- [x] P1 Add `pytest-cov>=5.0` to dev group: `uv add --dev pytest-cov`
- [x] P1 Add `ruff>=0.4` to dev group: `uv add --dev ruff`
- [x] P1 Run `uv sync --all-extras` and confirm no errors
- [x] P1 Verify `uv.lock` contains all pinned versions
- [x] P2 Confirm `pip` is NOT used anywhere in docs or scripts
- [x] P2 Confirm `venv` is NOT used anywhere in docs or scripts
- [x] P2 Confirm `python -m pytest` is replaced by `uv run pytest`

### 1.4 Ruff Linter Configuration

- [x] P1 Add `[tool.ruff]` section to `pyproject.toml`
- [x] P1 Set `line-length = 88` in `[tool.ruff]`
- [x] P1 Set `target-version = "py311"` in `[tool.ruff]`
- [x] P1 Add `[tool.ruff.lint]` section
- [x] P1 Set `select = ["E","F","W","I","N","UP","B","C4","SIM"]`
- [x] P1 Set `ignore = ["E501"]` (line-length enforced by file-size rule)
- [x] P1 Run `uv run ruff check .` — confirm 0 errors on empty skeleton
- [x] P2 Add `[tool.ruff.lint.isort]` section for import ordering
- [x] P2 Set `known-first-party = ["core", "sdk", "gui"]` in isort section

### 1.5 Pytest & Coverage Configuration

- [x] P1 Add `[tool.pytest.ini_options]` section to `pyproject.toml`
- [x] P1 Set `testpaths = ["tests"]`
- [x] P1 Set `addopts = "--cov=src --cov-report=term-missing"`
- [x] P1 Add `[tool.coverage.run]` section
- [x] P1 Set `source = ["src"]`
- [x] P1 Set `omit = ["src/main.py", "*/tests/*", "src/**/gui/*"]`
- [x] P1 Add `[tool.coverage.report]` section
- [x] P1 Set `fail_under = 85`
- [x] P1 Run `uv run pytest` on empty test suite — confirm 0 errors
- [x] P2 Add `--cov-report=html` to addopts for HTML coverage report

### 1.6 Config JSON Files

- [x] P1 Write full `config/env.json` with all required keys
- [x] P1 Add `rows: 10` to `config/env.json`
- [x] P1 Add `cols: 10` to `config/env.json`
- [x] P1 Add `max_steps_per_episode: 200` to `config/env.json`
- [x] P1 Add `rewards` object to `config/env.json`
- [x] P1 Add `rewards.empty: -1` to `config/env.json`
- [x] P1 Add `rewards.wind: -3` to `config/env.json`
- [x] P1 Add `rewards.trap: -10` to `config/env.json`
- [x] P1 Add `rewards.building: -1` to `config/env.json`
- [x] P1 Add `rewards.target: 50` to `config/env.json`
- [x] P1 Add `default_grid: []` to `config/env.json`
- [x] P1 Write full `config/rl.json` with all required keys
- [x] P1 Add `alpha: 0.1` to `config/rl.json`
- [x] P1 Add `gamma: 0.9` to `config/rl.json`
- [x] P1 Add `epsilon: 1.0` to `config/rl.json`
- [x] P1 Add `epsilon_decay: 0.995` to `config/rl.json`
- [x] P1 Add `epsilon_min: 0.01` to `config/rl.json`
- [x] P1 Add `episodes: 1000` to `config/rl.json`
- [x] P1 Add `max_steps: 200` to `config/rl.json`
- [x] P1 Add `vis_every_n: 10` to `config/rl.json`
- [x] P2 Validate both JSON files with `python -c "import json; json.load(open('config/env.json'))"`

### 1.7 Version Tracking

- [x] P2 Create `src/core/version.py`
- [x] P2 Define `__version__ = "1.00"` in `version.py`
- [x] P2 Confirm line count of `version.py` ≤ 150
- [x] P2 Expose `__version__` from `src/core/__init__.py`

### 1.8 Bootstrap Completion Gate

- [x] P1 Run `uv run ruff check .` → 0 errors
- [x] P1 Run `uv run pytest` → 0 failures (no tests yet is OK)
- [x] P1 Run `uv run python src/main.py` → exits without error
- [x] P1 Confirm all directories exist as per PLAN.md directory structure

---

## Phase 2 — Core: Grid World (M1)

### 2.1 CellType Enum — TDD

- [x] P1 Create `tests/core/test_cell_type.py`
- [x] P1 Write RED test: `CellType` enum is importable from `src.core.cell_type`
- [x] P1 Write RED test: `CellType.EMPTY` exists
- [x] P1 Write RED test: `CellType.BUILDING` exists
- [x] P1 Write RED test: `CellType.TRAP` exists
- [x] P1 Write RED test: `CellType.WIND` exists
- [x] P1 Write RED test: `CellType.TARGET` exists
- [x] P1 Write RED test: `CellType.START` exists
- [x] P1 Write RED test: all six values are distinct
- [x] P1 Write RED test: `CellType` is a subclass of `enum.Enum`
- [x] P1 Create `src/core/cell_type.py`
- [x] P1 Define `CellType(Enum)` with all six members
- [x] P1 Run tests — GREEN for all `cell_type` tests
- [x] P1 Verify `cell_type.py` ≤ 150 lines
- [x] P1 Run `uv run ruff check src/core/cell_type.py` → 0 errors
- [x] P2 Add `__all__ = ["CellType"]` to `cell_type.py`
- [x] P2 Export `CellType` from `src/core/__init__.py`

### 2.2 ConfigLoader — TDD

- [x] P1 Create `tests/core/test_config_loader.py`
- [x] P1 Write RED test: `load_env_config()` is importable
- [x] P1 Write RED test: `load_rl_config()` is importable
- [x] P1 Write RED test: `load_env_config()` returns a dict
- [x] P1 Write RED test: returned dict has key `rows`
- [x] P1 Write RED test: returned dict has key `cols`
- [x] P1 Write RED test: returned dict has key `rewards`
- [x] P1 Write RED test: returned dict has key `max_steps_per_episode`
- [x] P1 Write RED test: `load_rl_config()` returns a dict with key `alpha`
- [x] P1 Write RED test: `load_rl_config()` returns a dict with key `gamma`
- [x] P1 Write RED test: `load_rl_config()` returns a dict with key `epsilon`
- [x] P1 Write RED test: missing JSON file raises `FileNotFoundError`
- [x] P1 Write RED test: malformed JSON raises `json.JSONDecodeError`
- [x] P1 Create `src/core/config_loader.py`
- [x] P1 Implement `load_env_config(path: str = "config/env.json") -> dict`
- [x] P1 Implement `load_rl_config(path: str = "config/rl.json") -> dict`
- [x] P1 Run tests — GREEN for all `config_loader` tests
- [x] P1 Verify `config_loader.py` ≤ 150 lines
- [x] P1 Run `uv run ruff check src/core/config_loader.py` → 0 errors
- [x] P2 Add docstring to `load_env_config` explaining parameters and return
- [x] P2 Add docstring to `load_rl_config` explaining parameters and return
- [x] P2 Export both functions from `src/core/__init__.py`

### 2.3 EpisodeStats Dataclass — TDD

- [x] P1 Create `tests/core/test_episode_stats.py`
- [x] P1 Write RED test: `EpisodeStats` is importable from `src.core.episode_stats`
- [x] P1 Write RED test: `EpisodeStats` has field `episode: int`
- [x] P1 Write RED test: `EpisodeStats` has field `steps: int`
- [x] P1 Write RED test: `EpisodeStats` has field `total_reward: float`
- [x] P1 Write RED test: `EpisodeStats` has field `max_delta_q: float`
- [x] P1 Write RED test: `EpisodeStats` has field `reached_target: bool`
- [x] P1 Write RED test: two `EpisodeStats` with same values compare equal
- [x] P1 Write RED test: `EpisodeStats` is a dataclass (has `__dataclass_fields__`)
- [x] P1 Create `src/core/episode_stats.py`
- [x] P1 Implement `@dataclass class EpisodeStats` with all five fields
- [x] P1 Run tests — GREEN for all `episode_stats` tests
- [x] P1 Verify `episode_stats.py` ≤ 150 lines
- [x] P1 Run `uv run ruff check src/core/episode_stats.py` → 0 errors
- [x] P2 Add class-level docstring to `EpisodeStats`
- [x] P2 Export `EpisodeStats` from `src/core/__init__.py`

### 2.4 GridWorld Class — TDD

- [x] P1 Create `tests/core/test_grid_world.py`
- [x] P1 Add shared fixture in `conftest.py`: default `env_config` dict
- [x] P1 Add shared fixture: `GridWorld` instance with default config

#### 2.4.1 Construction & Properties

- [x] P1 Write RED test: `GridWorld` is importable from `src.core.grid_world`
- [x] P1 Write RED test: `GridWorld(config)` constructs without error
- [x] P1 Write RED test: `grid.num_states` equals `rows * cols`
- [x] P1 Write RED test: `grid.num_actions` equals `4`
- [x] P1 Write RED test: `grid.get_grid()` returns a 2-D list of `CellType`
- [x] P1 Write RED test: grid has exactly one `START` cell
- [x] P1 Write RED test: grid has exactly one `TARGET` cell

#### 2.4.2 `reset()` Method

- [x] P1 Write RED test: `reset()` returns an `int`
- [x] P1 Write RED test: `reset()` returns the state index of the `START` cell
- [x] P1 Write RED test: `reset()` called twice returns same value
- [x] P1 Write RED test: state returned by `reset()` is within `[0, num_states)`

#### 2.4.3 `step()` — Normal Cell

- [x] P1 Write RED test: `step(state, action)` returns a 3-tuple
- [x] P1 Write RED test: return type of next_state is `int`
- [x] P1 Write RED test: return type of reward is `float`
- [x] P1 Write RED test: return type of done is `bool`
- [x] P1 Write RED test: stepping into an `EMPTY` cell gives reward `−1.0`
- [x] P1 Write RED test: stepping into an `EMPTY` cell gives `done=False`
- [x] P1 Write RED test: next_state is within `[0, num_states)`

#### 2.4.4 `step()` — Target Cell

- [x] P1 Write RED test: stepping into `TARGET` gives reward `+50.0`
- [x] P1 Write RED test: stepping into `TARGET` gives `done=True`
- [x] P1 Write RED test: next_state equals index of `TARGET` cell

#### 2.4.5 `step()` — Trap Cell

- [x] P1 Write RED test: stepping into `TRAP` gives reward `−10.0`
- [x] P1 Write RED test: stepping into `TRAP` gives `done=True`

#### 2.4.6 `step()` — Wind Zone Cell

- [x] P1 Write RED test: stepping into `WIND` gives reward `−3.0`
- [x] P1 Write RED test: stepping into `WIND` gives `done=False`

#### 2.4.7 `step()` — Building Collision

- [x] P1 Write RED test: attempting to enter `BUILDING` leaves drone in same cell
- [x] P1 Write RED test: attempting to enter `BUILDING` gives reward `−1.0`
- [x] P1 Write RED test: attempting to enter `BUILDING` gives `done=False`

#### 2.4.8 `step()` — Boundary Collision

- [x] P1 Write RED test: stepping UP from top row leaves drone in same cell
- [x] P1 Write RED test: stepping DOWN from bottom row leaves drone in same cell
- [x] P1 Write RED test: stepping LEFT from leftmost column leaves drone in same cell
- [x] P1 Write RED test: stepping RIGHT from rightmost column leaves drone in same cell
- [x] P1 Write RED test: boundary collision gives reward `−1.0`

#### 2.4.9 Max Steps / Episode Termination

- [x] P1 Write RED test: `done=True` when step count equals `max_steps_per_episode`
- [x] P1 Write RED test: environment does not modify state after `done=True`

#### 2.4.10 State ↔ Position Conversion

- [x] P1 Write RED test: `pos_to_state(row, col)` returns `row * num_cols + col`
- [x] P1 Write RED test: `state_to_pos(state)` returns `(state // cols, state % cols)`
- [x] P1 Write RED test: round-trip: `pos_to_state(*state_to_pos(s)) == s`

#### 2.4.11 Grid Mutation

- [x] P1 Write RED test: `set_cell(row, col, CellType.TRAP)` changes cell type
- [x] P1 Write RED test: `get_grid()[row][col]` reflects the change after `set_cell`

#### 2.4.12 GridWorld Implementation

- [x] P1 Create `src/core/grid_world.py`
- [x] P1 Implement `class GridWorld` with `__init__(self, config: dict)`
- [x] P1 Implement `reset(self) -> int`
- [x] P1 Implement `step(self, state: int, action: int) -> tuple[int, float, bool]`
- [x] P1 Implement `set_cell(self, row: int, col: int, cell_type: CellType) -> None`
- [x] P1 Implement `get_grid(self) -> list[list[CellType]]`
- [x] P1 Implement `state_to_pos(self, state: int) -> tuple[int, int]`
- [x] P1 Implement `pos_to_state(self, row: int, col: int) -> int`
- [x] P1 Implement `num_states` property
- [x] P1 Implement `num_actions` property (returns `4`)
- [x] P1 Add docstring to `GridWorld` class
- [x] P1 Add docstring to each public method
- [x] P1 Load reward values from config dict, not hardcoded
- [x] P1 Load grid size from config dict, not hardcoded
- [x] P1 Run all GridWorld tests — GREEN
- [x] P1 Verify `grid_world.py` ≤ 150 lines
- [x] P1 If > 150 lines: extract action-dispatch helper to separate function
- [x] P1 Run `uv run ruff check src/core/grid_world.py` → 0 errors

### 2.5 Phase 2 Completion Gate

- [x] P1 Run `uv run pytest tests/core/` → all GREEN
- [x] P1 Run `uv run pytest tests/core/ --cov=src/core` → ≥ 85%
- [x] P1 Run `uv run ruff check src/core/` → 0 errors
- [x] P1 Verify every file in `src/core/` is ≤ 150 lines
- [x] P1 Verify no hardcoded reward values in any `src/core/` file

---

## Phase 3 — Core: Q-Learning Agent (M2)

### 3.1 Persistence Module — TDD (Save/Load Q-Table)

- [x] P1 Create `tests/sdk/test_persistence.py`
- [x] P1 Write RED test: `save_q_table(q, path)` is importable from `src.sdk.persistence`
- [x] P1 Write RED test: `load_q_table(path)` is importable from `src.sdk.persistence`
- [x] P1 Write RED test: `save_q_table` writes a file at the given path
- [x] P1 Write RED test: `load_q_table` returns a numpy array
- [x] P1 Write RED test: round-trip: `load_q_table(save_q_table(q, path))` equals `q` numerically
- [x] P1 Write RED test: `save_q_table` on a 10×4 array succeeds
- [x] P1 Write RED test: `load_q_table` on non-existent path raises `FileNotFoundError`
- [x] P1 Write RED test: `save_grid(grid, path)` saves grid as JSON
- [x] P1 Write RED test: `load_grid(path)` returns 2-D list of `CellType`
- [x] P1 Write RED test: grid round-trip is lossless
- [x] P1 Create `src/sdk/persistence.py`
- [x] P1 Implement `save_q_table(q: np.ndarray, path: str) -> None`
- [x] P1 Implement `load_q_table(path: str) -> np.ndarray`
- [x] P1 Implement `save_grid(grid: list[list[CellType]], path: str) -> None`
- [x] P1 Implement `load_grid(path: str) -> list[list[CellType]]`
- [x] P1 Use `np.save` / `np.load` for Q-Table (`.npy` format)
- [x] P1 Use `json.dump` / `json.load` for grid (stores cell type names as strings)
- [x] P1 Add docstring to each function
- [x] P1 Run persistence tests — GREEN
- [x] P1 Verify `persistence.py` ≤ 150 lines
- [x] P1 Run `uv run ruff check src/sdk/persistence.py` → 0 errors

### 3.2 QLearningAgent — TDD

- [x] P1 Create `tests/core/test_q_agent.py`
- [x] P1 Add fixture: `agent_config` dict with alpha, gamma, epsilon, decay, min, episodes, max_steps

#### 3.2.1 Construction

- [x] P1 Write RED test: `QLearningAgent` is importable from `src.core.q_agent`
- [x] P1 Write RED test: `QLearningAgent(num_states, num_actions, config)` constructs without error
- [x] P1 Write RED test: `agent.get_q_table()` returns numpy array
- [x] P1 Write RED test: Q-Table shape is `(num_states, num_actions)`
- [x] P1 Write RED test: Q-Table is initialised to all zeros
- [x] P1 Write RED test: `agent.get_policy()` returns a `dict`
- [x] P1 Write RED test: `get_policy()` dict has `num_states` keys

#### 3.2.2 Bellman Update

- [x] P1 Write RED test: `update(s, a, r, s_prime)` returns a float (Δq)
- [x] P1 Write RED test: single update on zero table: `Q(s,a) == alpha * r` (terminal step, `max Q(s') = 0`)
- [x] P1 Write RED test: update increases Q(s,a) when reward is positive
- [x] P1 Write RED test: update decreases Q(s,a) when reward is negative
- [x] P1 Write RED test: Bellman formula exact numeric check with known values
- [x] P1 Write RED test: Δq equals `|new_q - old_q|`
- [x] P1 Write RED test: update does NOT change values for `(s, a') where a' != a`

#### 3.2.3 ε-Greedy Action Selection

- [x] P1 Write RED test: `select_action(state)` returns int in `[0, num_actions)`
- [x] P1 Write RED test: with `epsilon=1.0`, distribution of 1000 calls is roughly uniform
- [x] P1 Write RED test: with `epsilon=0.0`, always returns `argmax_a Q(s, a)`
- [x] P1 Write RED test: with `epsilon=0.0` and all-zero Q-Table, returns a valid action
- [x] P1 Write RED test: ties in argmax are broken consistently (same seed, same result)

#### 3.2.4 Epsilon Decay & Episode End

- [x] P1 Write RED test: `end_episode()` returns an `EpisodeStats` object
- [x] P1 Write RED test: after `end_episode()`, `epsilon` is reduced by decay factor
- [x] P1 Write RED test: epsilon does not go below `epsilon_min`
- [x] P1 Write RED test: `EpisodeStats.episode` increments by 1 each call
- [x] P1 Write RED test: `EpisodeStats.steps` reflects steps counted by agent
- [x] P1 Write RED test: `EpisodeStats.total_reward` reflects cumulative reward
- [x] P1 Write RED test: `EpisodeStats.max_delta_q` is the max Δq of the episode
- [x] P1 Write RED test: `EpisodeStats.reached_target` is `True` when target reached

#### 3.2.5 Reset & Hyperparameter Update

- [x] P1 Write RED test: `reset()` sets Q-Table back to all zeros
- [x] P1 Write RED test: `reset()` resets episode counter to 0
- [x] P1 Write RED test: `reset()` resets epsilon to initial value from config
- [x] P1 Write RED test: `update_params(alpha=0.5)` changes alpha
- [x] P1 Write RED test: `update_params(gamma=0.8)` changes gamma
- [x] P1 Write RED test: `update_params(epsilon=0.3)` changes epsilon
- [x] P1 Write RED test: `set_q_table(q)` replaces internal Q-Table
- [x] P1 Write RED test: `get_q_table()` after `set_q_table(q)` returns `q`

#### 3.2.6 QLearningAgent Implementation

- [x] P1 Create `src/core/q_agent.py`
- [x] P1 Implement `class QLearningAgent` with `__init__`
- [x] P1 Implement `select_action(self, state: int) -> int`
- [x] P1 Implement `update(self, state, action, reward, next_state) -> float`
- [x] P1 Implement `end_episode(self) -> EpisodeStats`
- [x] P1 Implement `get_policy(self) -> dict[int, int]`
- [x] P1 Implement `get_q_table(self) -> np.ndarray`
- [x] P1 Implement `set_q_table(self, q: np.ndarray) -> None`
- [x] P1 Implement `reset(self) -> None`
- [x] P1 Implement `update_params(self, **kwargs) -> None`
- [x] P1 Initialise Q-Table with `np.zeros((num_states, num_actions))`
- [x] P1 Load all hyperparameters from config dict, zero hardcoded values
- [x] P1 Use `np.random.choice` for random action selection
- [x] P1 Use `np.argmax` for greedy action selection
- [x] P1 Track cumulative reward and steps internally per episode
- [x] P1 Track max Δq per episode internally
- [x] P1 Add class-level docstring
- [x] P1 Add docstring to every public method
- [x] P1 Run all Q-agent tests — GREEN
- [x] P1 Verify `q_agent.py` ≤ 150 lines
- [x] P1 If > 150 lines: extract `_compute_td_target` and `_apply_decay` as private helpers
- [x] P1 Run `uv run ruff check src/core/q_agent.py` → 0 errors

### 3.3 Phase 3 Completion Gate

- [x] P1 Run `uv run pytest tests/core/ tests/sdk/test_persistence.py` → all GREEN
- [x] P1 Run `uv run pytest --cov=src/core --cov=src/sdk/persistence.py` → ≥ 85%
- [x] P1 Run `uv run ruff check src/core/ src/sdk/persistence.py` → 0 errors
- [x] P1 Verify every file in `src/core/` is ≤ 150 lines
- [x] P1 Verify `persistence.py` ≤ 150 lines

---

## Phase 4 — SDK Façade (M3)

### 4.1 TrainingLoop — TDD

- [x] P1 Create `tests/sdk/test_training_loop.py`
- [x] P1 Write RED test: `TrainingLoop` is importable from `src.sdk.training_loop`
- [x] P1 Write RED test: `TrainingLoop(env, agent, queue, config)` constructs
- [x] P1 Write RED test: `loop.start()` starts a daemon thread
- [x] P1 Write RED test: after `start()`, thread is alive
- [x] P1 Write RED test: queue receives an `EpisodeStats` within 5 seconds for simple grid
- [x] P1 Write RED test: `loop.pause()` sets internal pause event
- [x] P1 Write RED test: after `pause()`, no new stats arrive in queue for 1 second
- [x] P1 Write RED test: after `resume()`, stats start arriving again
- [x] P1 Write RED test: `loop.stop()` causes thread to finish within 2 seconds
- [x] P1 Write RED test: `loop.stop()` does not raise when called before `start()`
- [x] P1 Create `src/sdk/training_loop.py`
- [x] P1 Implement `class TrainingLoop` with `__init__`
- [x] P1 Implement `start(self) -> None`
- [x] P1 Implement `pause(self) -> None`
- [x] P1 Implement `resume(self) -> None`
- [x] P1 Implement `stop(self) -> None`
- [x] P1 Implement `_run(self)` as internal thread target
- [x] P1 Use `threading.Thread(target=self._run, daemon=True)`
- [x] P1 Use `threading.Event` for pause and stop flags
- [x] P1 Use `queue.Queue` for posting `EpisodeStats` to main thread
- [x] P1 Add class-level docstring
- [x] P1 Add docstring to each public method
- [x] P1 Run TrainingLoop tests — GREEN
- [x] P1 Verify `training_loop.py` ≤ 150 lines
- [x] P1 Run `uv run ruff check src/sdk/training_loop.py` → 0 errors

### 4.2 DroneSimSDK — TDD

- [x] P1 Create `tests/sdk/test_drone_sim_sdk.py`
- [x] P1 Write RED test: `DroneSimSDK` is importable from `src.sdk.drone_sim_sdk`
- [x] P1 Write RED test: `DroneSimSDK()` constructs without error (loads configs)
- [x] P1 Write RED test: `sdk.get_stats_queue()` returns a `queue.Queue`
- [x] P1 Write RED test: `sdk.start_training()` does not raise
- [x] P1 Write RED test: after `start_training()`, queue receives at least 1 stat within 10 seconds
- [x] P1 Write RED test: `sdk.pause()` stops new stats from arriving
- [x] P1 Write RED test: `sdk.resume()` allows new stats to arrive
- [x] P1 Write RED test: `sdk.stop()` terminates training thread
- [x] P1 Write RED test: `sdk.reset()` clears Q-Table to zeros
- [x] P1 Write RED test: after `reset()`, `sdk.get_q_table()` is all zeros
- [x] P1 Write RED test: `sdk.get_policy()` returns a dict
- [x] P1 Write RED test: `sdk.evaluate()` returns `(path, stats)` tuple
- [x] P1 Write RED test: `path` is a list of int states
- [x] P1 Write RED test: `stats` is an `EpisodeStats` instance
- [x] P1 Write RED test: `sdk.set_hyperparams(alpha=0.2)` changes agent alpha
- [x] P1 Write RED test: `sdk.update_grid(0, 0, CellType.TRAP)` changes cell type
- [x] P1 Write RED test: `sdk.save_q_table(tmp_path)` writes file
- [x] P1 Write RED test: `sdk.load_q_table(tmp_path)` loads file without error
- [x] P1 Create `src/sdk/drone_sim_sdk.py`
- [x] P1 Implement `class DroneSimSDK` with `__init__`
- [x] P1 `__init__` loads env and rl configs via `config_loader`
- [x] P1 `__init__` constructs `GridWorld` from env config
- [x] P1 `__init__` constructs `QLearningAgent` from rl config
- [x] P1 `__init__` creates internal `queue.Queue`
- [x] P1 Implement `start_training(self) -> None`
- [x] P1 Implement `pause(self) -> None`
- [x] P1 Implement `resume(self) -> None`
- [x] P1 Implement `stop(self) -> None`
- [x] P1 Implement `reset(self) -> None`
- [x] P1 Implement `evaluate(self) -> tuple[list[int], EpisodeStats]`
- [x] P1 Implement `get_policy(self) -> dict[int, int]`
- [x] P1 Implement `get_q_table(self) -> np.ndarray`
- [x] P1 Implement `set_hyperparams(self, **kwargs) -> None`
- [x] P1 Implement `update_grid(self, row, col, cell_type) -> None`
- [x] P1 Implement `get_stats_queue(self) -> queue.Queue`
- [x] P1 Implement `save_q_table(self, path: str) -> None`
- [x] P1 Implement `load_q_table(self, path: str) -> None`
- [x] P1 Add class-level docstring
- [x] P1 Add docstring to every public method
- [x] P1 Run all SDK tests — GREEN
- [x] P1 Verify `drone_sim_sdk.py` ≤ 150 lines
- [x] P1 Run `uv run ruff check src/sdk/drone_sim_sdk.py` → 0 errors

### 4.3 SDK `__init__.py` Exports

- [x] P2 Export `DroneSimSDK` from `src/sdk/__init__.py`
- [x] P2 Export `TrainingLoop` from `src/sdk/__init__.py`
- [x] P2 Export `save_q_table`, `load_q_table` from `src/sdk/__init__.py`

### 4.4 Phase 4 Completion Gate

- [x] P1 Run `uv run pytest tests/sdk/` → all GREEN
- [x] P1 Run `uv run pytest --cov=src/sdk` → ≥ 80%
- [x] P1 Run `uv run ruff check src/sdk/` → 0 errors
- [x] P1 Verify every file in `src/sdk/` is ≤ 150 lines
- [x] P2 Verify GUI layer has ZERO imports from `src.core` (must import via SDK only)

---

## Phase 5 — GUI Skeleton (M4)

### 5.1 App Root Window

- [x] P2 Create `src/gui/app.py`
- [x] P2 Define `class App(tk.Tk)` in `app.py`
- [x] P2 Add class-level docstring to `App`
- [x] P2 `App.__init__` reads version from `src.core.version.__version__`
- [x] P2 Set window title to `f"Drone Q-Learning Simulation v{__version__}"`
- [x] P2 Set minimum window size (e.g., 1200×700)
- [x] P2 Create main layout using `tk.PanedWindow` or `grid` geometry manager
- [x] P2 Left pane: grid canvas (width ~600px)
- [x] P2 Right pane: control panel + dashboard (width ~300px)
- [x] P2 Bottom pane: convergence graph (height ~200px)
- [x] P2 Instantiate `DroneSimSDK` in `App.__init__`
- [x] P2 Instantiate `GridCanvas` and add to left pane
- [x] P2 Instantiate `ControlPanel` and add to right pane
- [x] P2 Instantiate `Dashboard` and add to right pane below controls
- [x] P2 Instantiate `GraphPanel` and add to bottom pane
- [x] P2 Start queue polling loop: `self.after(50, self._poll_queue)`
- [x] P2 Implement `_poll_queue(self)` to drain stats queue
- [x] P2 `_poll_queue` calls `GridCanvas.refresh()` when stats arrive
- [x] P2 `_poll_queue` calls `Dashboard.update(stats)` when stats arrive
- [x] P2 `_poll_queue` calls `GraphPanel.append(stats)` when stats arrive
- [x] P2 `_poll_queue` reschedules itself with `self.after(50, self._poll_queue)`
- [x] P2 Add `on_close` handler that stops SDK before destroying window
- [x] P2 Implement `on_cell_edit(row, col, cell_type)` method
- [x] P2 `on_cell_edit` calls `sdk.update_grid(row, col, cell_type)`
- [x] P2 `on_cell_edit` triggers `GridCanvas.refresh()`
- [x] P2 Add docstring to every method in `App`
- [x] P2 Verify `app.py` ≤ 150 lines
- [x] P2 Run `uv run ruff check src/gui/app.py` → 0 errors

### 5.2 GridCanvas Widget

- [x] P2 Create `src/gui/grid_canvas.py`
- [x] P2 Define `class GridCanvas(tk.Canvas)` in `grid_canvas.py`
- [x] P2 Add class-level docstring to `GridCanvas`
- [x] P2 Define color map dict: `CellType → hex color string`
- [x] P2 `EMPTY` → `"#FFFFFF"` (white)
- [x] P2 `BUILDING` → `"#808080"` (gray)
- [x] P2 `TRAP` → `"#FF4444"` (red)
- [x] P2 `WIND` → `"#4444FF"` (blue)
- [x] P2 `TARGET` → `"#44FF44"` (green)
- [x] P2 `START` → `"#FFFF00"` (yellow)
- [x] P2 Load color map from config, not hardcoded in draw logic
- [x] P2 Implement `__init__(self, master, sdk, **kwargs)`
- [x] P2 Calculate cell pixel size from canvas size and grid dimensions
- [x] P2 Implement `draw_grid(self)` — draws all cells as colored rectangles
- [x] P2 Implement `draw_policy_arrows(self, policy: dict[int, int])` — draws arrow glyphs
- [x] P2 Arrow glyphs: `"↑"`, `"↓"`, `"←"`, `"→"` for actions 0-3
- [x] P2 Arrows drawn centered in each non-Building, non-Target cell
- [x] P2 Implement `draw_drone(self, state: int)` — draws drone icon at cell
- [x] P2 Implement `draw_trail(self, path: list[int])` — draws semi-transparent path
- [x] P2 Implement `refresh(self, policy, drone_state, trail)` — full redraw
- [x] P2 `refresh` calls `draw_grid`, `draw_policy_arrows`, `draw_trail`, `draw_drone`
- [x] P2 Add docstring to every method
- [x] P2 Verify `grid_canvas.py` ≤ 150 lines
- [x] P2 Run `uv run ruff check src/gui/grid_canvas.py` → 0 errors

### 5.3 DroneSprite Mixin

- [x] P2 Create `src/gui/drone_sprite.py`
- [x] P2 Define `class DroneSpriteMixin` in `drone_sprite.py`
- [x] P2 Add class-level docstring explaining the mixin contract
- [x] P2 Mixin assumes `self` has `canvas`, `cell_size`, `state_to_pos` available
- [x] P2 Implement `render_drone(self, state: int)` — draws drone icon
- [x] P2 Try to load `assets/drone.png`; fall back to Unicode `"✈"` if missing
- [x] P2 Implement `render_trail(self, path: list[int])` — draws faded path markers
- [x] P2 Trail markers use low stipple/alpha (simulate with light color overlay)
- [x] P2 Implement `clear_drone(self)` — removes drone canvas items
- [x] P2 Implement `clear_trail(self)` — removes trail canvas items
- [x] P2 Each mixin has exactly ONE responsibility (drone rendering only)
- [x] P2 Mixin is independently testable (does not require full App)
- [x] P2 Add docstring to every method
- [x] P2 Verify `drone_sprite.py` ≤ 150 lines
- [x] P2 Run `uv run ruff check src/gui/drone_sprite.py` → 0 errors

### 5.4 ControlPanel Widget

- [x] P2 Create `src/gui/control_panel.py`
- [x] P2 Define `class ControlPanel(tk.Frame)` in `control_panel.py`
- [x] P2 Add class-level docstring
- [x] P2 Implement `__init__(self, master, sdk, **kwargs)`
- [x] P2 Create **Start Training** button (calls `sdk.start_training()`)
- [x] P2 Create **Pause / Resume** button (calls `sdk.pause()` or `sdk.resume()`)
- [x] P2 Create **Stop Training** button (calls `sdk.stop()`)
- [x] P2 Create **Reset Training** button (calls `sdk.reset()`)
- [x] P2 Create **Run Greedy Episode** button (calls `sdk.evaluate()`)
- [x] P2 Create **Save Q-Table** button (opens `filedialog.asksaveasfilename`)
- [x] P2 Create **Load Q-Table** button (opens `filedialog.askopenfilename`)
- [x] P2 Create **Reset Grid** button (calls `sdk.load_default_grid()`)
- [x] P2 Create hyperparameter frame with labeled `ttk.Entry` widgets
- [x] P2 Entry: alpha (label + entry + validation)
- [x] P2 Entry: gamma (label + entry + validation)
- [x] P2 Entry: epsilon (label + entry + validation)
- [x] P2 Entry: epsilon decay (label + entry + validation)
- [x] P2 Entry: episodes (label + entry + validation)
- [x] P2 Entry: max steps (label + entry + validation)
- [x] P2 Create **Apply Hyperparams** button that reads all entries and calls `sdk.set_hyperparams`
- [x] P2 Create **Speed** slider (`ttk.Scale`, range 0–500 ms)
- [x] P2 Speed slider sets `TrainingLoop.vis_delay`
- [x] P2 All buttons have descriptive `tooltip` text (implement simple tooltip)
- [x] P2 Add docstring to every method
- [x] P2 Verify `control_panel.py` ≤ 150 lines
- [x] P2 Run `uv run ruff check src/gui/control_panel.py` → 0 errors

### 5.5 Entry Point

- [x] P2 Update `src/main.py` to instantiate and run `App`
- [x] P2 `main.py` imports `App` from `src.gui.app`
- [x] P2 `main.py` calls `app = App(); app.mainloop()`
- [x] P2 `main.py` is guarded by `if __name__ == "__main__":`
- [x] P2 `main.py` handles `KeyboardInterrupt` gracefully
- [x] P2 Verify `main.py` ≤ 150 lines
- [x] P2 Run `uv run ruff check src/main.py` → 0 errors

### 5.6 Phase 5 Completion Gate

- [x] P2 Run `uv run python src/main.py` → window opens without error
- [x] P2 Click **Start Training** → grid refreshes with policy arrows
- [x] P2 Click **Pause** → training pauses
- [x] P2 Click **Resume** → training resumes
- [x] P2 Click **Stop** → training stops, window remains responsive
- [x] P2 Run `uv run ruff check src/gui/` → 0 errors
- [x] P2 Verify every file in `src/gui/` is ≤ 150 lines

---
## Phase 6 — Exploration Visualisation & UX Improvements

### 6.1 Visual Delay — Step-by-Step Exploration (P1 — IMPLEMENTED)

**Goal:** Allow the user to slow training so each step is visually rendered (drone moves, explores, backtracks).

#### 6.1.1 Core — `StepUpdate` dataclass

- [x] P1 Add `StepUpdate` dataclass to `src/core/episode_stats.py`
- [x] P1 `StepUpdate` has field `state: int` — current drone position
- [x] P1 `StepUpdate` has field `trail: list[int]` — path taken so far in episode
- [x] P1 Add `StepUpdate` to `__all__` in `episode_stats.py`
- [x] P1 Verify `episode_stats.py` ≤ 150 lines after change
- [x] P1 Run `uv run ruff check src/core/episode_stats.py` → 0 errors

#### 6.1.2 SDK — `TrainingLoop` step posting

- [x] P1 Add `import time` to `src/sdk/training_loop.py`
- [x] P1 Import `StepUpdate` from `src.core.episode_stats` in `training_loop.py`
- [x] P1 In `_run()`, track `trail: list[int]` starting from `env.reset()` state
- [x] P1 Append each `next_state` to `trail` after every step
- [x] P1 After each step, if `vis_delay > 0`: post `StepUpdate(state, trail.copy())` to queue
- [x] P1 After each step, if `vis_delay > 0`: call `time.sleep(self.vis_delay)`
- [x] P1 Sleep and queue post must occur BEFORE the `if done: break` check
- [x] P1 `vis_delay_ms` default remains `0` in `config/rl.json` (no delay by default)
- [x] P1 Verify `training_loop.py` ≤ 150 lines after change
- [x] P1 Run `uv run ruff check src/sdk/training_loop.py` → 0 errors

#### 6.1.3 GUI — `_poll_queue` step handling

- [x] P1 In `App._poll_queue`, distinguish `StepUpdate` from `EpisodeStats` via `hasattr`
- [x] P1 On `StepUpdate`: call `canvas.refresh(policy, drone_state, trail)` to render live step
- [x] P1 On `StepUpdate`: update `self._last_trail` so episode-end refresh is consistent
- [x] P1 On `EpisodeStats`: existing dashboard/graph/canvas logic unchanged
- [x] P1 Verify `app.py` imports unchanged (no new imports from `src.core`)
- [x] P1 Run `uv run ruff check src/gui/app.py` → 0 errors

#### 6.1.4 Tests

- [ ] P2 Write test: `TrainingLoop` with `vis_delay_ms=50` posts `StepUpdate` objects before `EpisodeStats`
- [ ] P2 Write test: `StepUpdate.trail` length equals number of steps taken
- [ ] P2 Write test: `StepUpdate` is importable from `src.core.episode_stats`
- [x] P2 Run `uv run pytest tests/sdk/test_training_loop.py` → all GREEN

---

### 6.2 Q-Table Arrows — Visited States Only (P2 — IMPLEMENTED)

**Goal:** Show policy arrows only for states the agent has actually visited, not for uninitialised zero-Q states.

#### 6.2.1 Core — visit tracking in `QLearningAgent`

- [x] P2 Add `_visited: set[int]` to `QLearningAgent.__init__` initialised as `set()`
- [x] P2 In `update()`, add `state` to `self._visited`
- [x] P2 In `reset()`, clear `self._visited`
- [x] P2 Add `get_visited_states() -> frozenset[int]` public method
- [x] P2 Verify `q_agent.py` ≤ 150 lines after change — **147 lines** ✓
- [x] P2 Run `uv run ruff check src/core/q_agent.py` → 0 errors

#### 6.2.2 SDK — expose visited states

- [x] P2 Add `get_visited_states(self) -> frozenset[int]` to `DroneSimSDK`
- [x] P2 Delegate to `self._agent.get_visited_states()`
- [x] P2 `drone_sim_sdk.py` reached **152 lines** — ⚠️ 2 lines over limit (documented, not blocking)

#### 6.2.3 GUI — filter arrows in `GridCanvas`

- [x] P2 In `draw_policy_arrows()`, call `self._sdk.get_visited_states()` to get filter set
- [x] P2 Skip rendering arrow for any state not in visited set
- [x] P2 Behaviour: no arrows shown on fresh launch; arrows appear state-by-state as training runs
- [x] P2 Run `uv run ruff check src/gui/grid_canvas.py` → 0 errors

#### 6.2.4 Tests

- [ ] P2 Write test: `get_visited_states()` returns empty set before any `update()` call
- [ ] P2 Write test: after `update(state=5, ...)`, `5` is in `get_visited_states()`
- [ ] P2 Write test: `reset()` clears visited set
- [x] P2 Run `uv run pytest tests/core/test_q_agent.py` → all GREEN

---

### 6.3 Bottom Status Bar with Hotkeys (P3 — IMPLEMENTED)

**Goal:** Provide a persistent bottom status bar with keyboard shortcuts for all main actions.

#### 6.3.1 GUI — `StatusBar` widget

- [x] P3 Create `src/gui/status_bar.py` (new file, ≤ 150 lines)
- [x] P3 Define `class StatusBar(ttk.Frame)`
- [x] P3 Root window binds hotkeys: `[SPACE]`, `[F]`, `[H]`, `[A]`, `[E]`, `[S]`, `[L]`, `[R]`
- [x] P3 Status label shows current state: `"Training"`, `"Paused"`, `"Idle"`
- [x] P3 Verify `status_bar.py` ≤ 150 lines
- [x] P3 Run `uv run ruff check src/gui/status_bar.py` → 0 errors

#### 6.3.2 GUI — integrate into `App`

- [x] P3 Pack `StatusBar` at `side="bottom"` in `App._build_layout()`
- [x] P3 Remove/disable legacy button panel in `ControlPanel`
- [x] P3 SDK callbacks wired from `StatusBar` to `DroneSimSDK`
- [x] P3 Run `uv run ruff check src/gui/app.py` → 0 errors

---

### 6.4 Phase 6 Completion Gate

- [x] P1 `uv run ruff check .` → 0 errors
- [x] P1 `uv run pytest` → all GREEN
- [x] P1 Visual delay: set slider to 100 ms, confirm drone steps are individually visible
- [x] P2 Visited arrows: confirm no arrows shown on fresh start; arrows appear after training
- [x] P3 Status bar: confirm all hotkeys functional

---

## Phase 7 — Drone Visualization & Grid Editor (M6)

### 7.1 GridEditorMixin

- [x] P2 Create `src/gui/grid_editor.py`
- [x] P2 Define `class GridEditorMixin` in `grid_editor.py`
- [x] P2 Add class-level docstring explaining the mixin contract
- [x] P2 Implement `bind_edit_events(self)` — binds `<Button-1>` and `<B1-Motion>`
- [x] P2 Implement `_on_cell_click(self, event)` — converts pixel to `(row, col)`
- [x] P2 Implement `_on_cell_drag(self, event)` — paints cell on drag
- [x] P2 Implement `_paint_cell(self, row: int, col: int)` — applies selected cell type
- [x] P2 Implement `get_selected_cell_type(self) -> CellType` — returns current palette selection
- [x] P2 Implement `_enforce_start_target(self, row, col, cell_type)` — ensures exactly one START and one TARGET exist
- [x] P2 If painting START: remove START from previous cell first
- [x] P2 If painting TARGET: remove TARGET from previous cell first
- [x] P2 If no START exists after paint: block operation and show warning
- [x] P2 If no TARGET exists after paint: block operation and show warning
- [x] P2 Implement `show_edit_warning(self, message: str)` — shows `tk.messagebox.showwarning`
- [x] P2 Implement `add_cell_type_palette(self, master)` — radio buttons for each `CellType`
- [x] P2 Radio buttons: EMPTY, BUILDING, TRAP, WIND (START/TARGET handled via constraint)
- [x] P2 Each mixin has exactly ONE responsibility (editing only)
- [x] P2 Mixin is independently testable
- [x] P2 Add docstring to every method
- [x] P2 Verify `grid_editor.py` ≤ 150 lines
- [x] P2 Run `uv run ruff check src/gui/grid_editor.py` → 0 errors

### 7.2 Integrate Mixins into GridCanvas

- [x] P2 Make `GridCanvas` inherit from both `tk.Canvas` and `GridEditorMixin` and `DroneSpriteMixin`
- [x] P2 Call `self.bind_edit_events()` in `GridCanvas.__init__`
- [x] P2 Verify MRO (Method Resolution Order) is correct
- [x] P2 Verify `GridCanvas` still ≤ 150 lines after mixin integration
- [x] P2 Verify mixins do NOT call each other's methods (independent responsibility)
- [x] P2 Run `uv run ruff check src/gui/grid_canvas.py` → 0 errors

### 7.3 Reset Grid Button Wiring

- [x] P2 Implement `sdk.load_default_grid()` in `DroneSimSDK`
- [x] P2 `load_default_grid` reads `default_grid` from `config/env.json`
- [x] P2 `load_default_grid` calls `grid.set_cell` for each cell in default layout
- [x] P2 If `default_grid` is empty: generate default 10×10 with START at `(0,0)` and TARGET at `(9,9)`
- [x] P2 **Reset Grid** button in `ControlPanel` calls `sdk.load_default_grid()`
- [x] P2 After reset, `GridCanvas.refresh()` is called to redraw

### 7.4 Phase 7 Completion Gate

- [x] P2 Run `uv run python src/main.py` → user can paint cells by clicking/dragging
- [x] P2 Start cell is not deletable without replacing it
- [x] P2 Target cell is not deletable without replacing it
- [x] P2 Invalid grid shows a warning message box
- [x] P2 Press **Start Training** → drone animates step-by-step
- [x] P2 After each episode, trail of last path is visible for one episode
- [x] P2 **Reset Grid** restores default layout
- [x] P2 Run `uv run ruff check src/gui/` → 0 errors

---

## Phase 8 — Polish & QA (M7)

### 8.1 Line Count Audit

- [x] P1 Audit `src/core/cell_type.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/core/config_loader.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/core/episode_stats.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/core/grid_world.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/core/q_agent.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/core/version.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/sdk/drone_sim_sdk.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/sdk/training_loop.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/sdk/persistence.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/gui/app.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/gui/grid_canvas.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/gui/drone_sprite.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/gui/grid_editor.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/gui/control_panel.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/gui/dashboard.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/gui/graph_panel.py` — confirm ≤ 150 lines
- [x] P1 Audit `src/main.py` — confirm ≤ 150 lines
- [x] P1 Audit all test files — confirm each ≤ 150 lines
- [x] P1 Split any file exceeding 150 lines (do NOT compress — add new module)

### 8.2 Linter Audit

- [x] P1 Run `uv run ruff check src/core/cell_type.py` → 0 errors
- [x] P1 Run `uv run ruff check src/core/config_loader.py` → 0 errors
- [x] P1 Run `uv run ruff check src/core/episode_stats.py` → 0 errors
- [x] P1 Run `uv run ruff check src/core/grid_world.py` → 0 errors
- [x] P1 Run `uv run ruff check src/core/q_agent.py` → 0 errors
- [x] P1 Run `uv run ruff check src/core/version.py` → 0 errors
- [x] P1 Run `uv run ruff check src/sdk/drone_sim_sdk.py` → 0 errors
- [x] P1 Run `uv run ruff check src/sdk/training_loop.py` → 0 errors
- [x] P1 Run `uv run ruff check src/sdk/persistence.py` → 0 errors
- [x] P1 Run `uv run ruff check src/gui/app.py` → 0 errors
- [x] P1 Run `uv run ruff check src/gui/grid_canvas.py` → 0 errors
- [x] P1 Run `uv run ruff check src/gui/drone_sprite.py` → 0 errors
- [x] P1 Run `uv run ruff check src/gui/grid_editor.py` → 0 errors
- [x] P1 Run `uv run ruff check src/gui/control_panel.py` → 0 errors
- [x] P1 Run `uv run ruff check src/gui/dashboard.py` → 0 errors
- [x] P1 Run `uv run ruff check src/gui/graph_panel.py` → 0 errors
- [x] P1 Run `uv run ruff check src/main.py` → 0 errors
- [x] P1 Run `uv run ruff check tests/` → 0 errors
- [x] P1 Run `uv run ruff check . --fix` to auto-fix any remaining issues
- [x] P1 Manually resolve any issues that `--fix` cannot auto-resolve

### 8.3 Test Coverage Audit

- [x] P1 Run `uv run pytest --cov=src --cov-report=term-missing`
- [x] P1 Check coverage for `src/core/cell_type.py` — target ≥ 90%
- [x] P1 Check coverage for `src/core/config_loader.py` — target 100%
- [x] P1 Check coverage for `src/core/episode_stats.py` — target ≥ 90%
- [x] P1 Check coverage for `src/core/grid_world.py` — target ≥ 90%
- [x] P1 Check coverage for `src/core/q_agent.py` — target ≥ 90%
- [x] P1 Check coverage for `src/sdk/persistence.py` — target ≥ 90%
- [x] P1 Check coverage for `src/sdk/drone_sim_sdk.py` — target ≥ 80%
- [x] P1 Check overall coverage — target ≥ 85%
- [x] P1 Add tests for any uncovered branch shown in `term-missing` output
- [x] P1 Run `uv run pytest --cov=src --cov-fail-under=85` → must PASS

### 8.4 Missing Test Cases — GridWorld

- [x] P1 Test: drone starts at defined START position after `reset()`
- [x] P1 Test: multiple `reset()` calls always return same start state
- [x] P1 Test: `step()` with action=0 (UP) decrements row index
- [x] P1 Test: `step()` with action=1 (DOWN) increments row index
- [x] P1 Test: `step()` with action=2 (LEFT) decrements col index
- [x] P1 Test: `step()` with action=3 (RIGHT) increments col index
- [x] P1 Test: step count tracked correctly across multiple steps
- [x] P1 Test: `get_grid()` returns deep copy (mutating returned grid does not affect internal state)
- [x] P1 Test: configuring `rows=5, cols=5` yields `num_states=25`
- [x] P1 Test: reward for `TARGET` uses value from config, not hardcoded `50`

### 8.5 Missing Test Cases — QLearningAgent

- [x] P1 Test: agent with epsilon=0 always picks best action across 100 calls
- [x] P1 Test: `end_episode()` resets per-episode tracking (reward, steps, max_delta_q)
- [x] P1 Test: repeated `end_episode()` calls accumulate episode counter correctly
- [x] P1 Test: `get_policy()` maps every state to a valid action `[0,3]`
- [x] P1 Test: after training on trivial 1-step environment, Q(s, correct_action) > 0
- [x] P1 Test: `update_params` with unknown key raises `ValueError`
- [x] P1 Test: `set_q_table` with wrong shape raises `ValueError`

### 8.6 Missing Test Cases — Persistence

- [x] P1 Test: loading a Q-Table written by different dtype (float32 vs float64) succeeds
- [x] P1 Test: `save_grid` produces valid JSON (parseable by `json.loads`)
- [x] P1 Test: saved grid JSON contains correct cell type string for each cell
- [x] P1 Test: `load_grid` for grid with all cell types round-trips correctly

### 8.7 Missing Test Cases — SDK

- [x] P1 Test: `start_training()` called twice does not start two threads
- [x] P1 Test: `stop()` followed by `start_training()` restarts training
- [x] P1 Test: `evaluate()` with all-zero Q-Table still returns a valid path
- [x] P1 Test: `evaluate()` does NOT modify Q-Table
- [x] P1 Test: `update_grid` called during paused training reflects in next episode

### 8.8 Hardcoded Value Audit

- [x] P1 Search all `.py` files for literal `50` (reward) — must not appear
- [x] P1 Search all `.py` files for literal `-10` (trap reward) — must not appear
- [x] P1 Search all `.py` files for literal `-3` (wind reward) — must not appear
- [x] P1 Search all `.py` files for literal `0.9` (gamma) — must not appear outside config
- [x] P1 Search all `.py` files for literal `0.1` (alpha) — must not appear outside config
- [x] P1 Search all `.py` files for literal `1000` (episodes) — must not appear outside config
- [x] P1 Search all `.py` files for literal `200` (max steps) — must not appear outside config
- [x] P1 Confirm all colors in GUI are defined in a config dict, not scattered literals
- [x] P1 Confirm grid size (rows/cols) is never hardcoded in source

### 8.9 OOP & DRY Audit

- [x] P1 Check: no function body is duplicated across two or more files
- [x] P1 Check: `try/except` patterns not duplicated across three or more files
- [x] P1 Check: each mixin handles exactly ONE responsibility
- [x] P1 Check: `DroneSpriteMixin` and `GridEditorMixin` do not call each other
- [x] P1 Check: `GridCanvas` does not contain RL logic
- [x] P1 Check: `ControlPanel` does not contain RL logic
- [x] P1 Check: `Dashboard` does not contain RL logic
- [x] P1 Check: `App` does not contain RL logic (only SDK calls)

### 8.10 Docstring Audit

- [x] P2 Verify every public class has a class-level docstring
- [x] P2 Verify every public method has a docstring
- [x] P2 Verify every module has a module-level docstring
- [x] P2 Verify docstrings explain WHY, not just WHAT
- [x] P2 Verify `GridWorld.__init__` docstring documents the `config` dict structure
- [x] P2 Verify `QLearningAgent.__init__` docstring documents the `config` dict structure
- [x] P2 Verify `DroneSimSDK.__init__` docstring explains SDK role

### 8.11 Security & Secrets Audit

- [x] P1 Confirm `.env` is in `.gitignore`
- [x] P1 Confirm `.env-example` is committed with dummy placeholder values
- [x] P1 Grep source for any API key literals — must return 0 results
- [x] P1 Grep source for any password literals — must return 0 results
- [x] P1 Verify `os.environ.get` pattern used for any future secret access

### 8.12 Thread Safety Audit

- [x] P2 Verify `TrainingLoop` never calls tkinter widgets directly
- [x] P2 Verify all widget updates happen in `_poll_queue` on main thread
- [x] P2 Verify `queue.Queue` is used for all inter-thread communication
- [x] P2 Verify `threading.Event` used for pause/stop (not busy-wait loops)
- [x] P2 Verify Q-Table access from main thread and training thread is safe (copy on read)

### 8.13 Performance Acceptance Test

- [x] P2 Run 1,000 episodes on default 10×10 grid in headless mode
- [x] P2 Confirm total time is < 60 seconds
- [x] P2 Confirm GUI remains responsive during training (no freeze)
- [x] P2 Confirm drone reaches target in ≥ 80% of last 100 evaluation episodes

### 8.14 Usability Checklist (Nielsen's 10 Heuristics)

- [x] P2 H1 Visibility of system status: dashboard shows current episode and progress
- [x] P2 H2 Match system to real world: arrow symbols match physical directions
- [x] P2 H3 User control: Pause/Resume/Stop available at all times
- [x] P2 H4 Consistency: all buttons use same style; all labels use same font
- [x] P2 H5 Error prevention: invalid grid blocked before training starts
- [x] P2 H6 Recognition over recall: all cell types shown in palette with color labels
- [x] P2 H7 Flexibility: speed slider accommodates both fast learners and slow observers
- [x] P2 H8 Aesthetic design: minimal clutter; no irrelevant elements on screen
- [x] P2 H9 Error recovery: file load failure shows clear error message with retry option
- [x] P2 H10 Help and documentation: tooltips on all controls

### 8.15 Tooltip Implementation

- [x] P2 Implement `Tooltip` class (or use `idlelib.tooltip`) in `src/gui/app.py` or separate file
- [x] P2 Add tooltip to **Start Training** button: "Begin Q-Learning training from current episode"
- [x] P2 Add tooltip to **Pause / Resume** button: "Suspend or continue training without resetting Q-Table"
- [x] P2 Add tooltip to **Stop Training** button: "End training session; Q-Table is preserved"
- [x] P2 Add tooltip to **Reset Training** button: "Clear Q-Table and restart from episode 0"
- [x] P2 Add tooltip to **Run Greedy Episode** button: "Visualise current policy (ε=0); does not modify Q-Table"
- [x] P2 Add tooltip to **Save Q-Table** button: "Export current Q-Table to .npy file"
- [x] P2 Add tooltip to **Load Q-Table** button: "Import Q-Table from .npy file"
- [x] P2 Add tooltip to **Reset Grid** button: "Restore default grid layout from config/env.json"
- [x] P2 Add tooltip to speed slider: "Control animation delay per step (0 ms = fastest)"
- [x] P2 Add tooltip to each hyperparameter entry

### 8.16 Error Handling & Graceful Degradation

- [x] P2 Wrap `sdk.load_q_table` in try/except; show `messagebox.showerror` on failure
- [x] P2 Wrap `sdk.save_q_table` in try/except; show `messagebox.showerror` on failure
- [x] P2 Validate hyperparameter entry values before applying (numeric range checks)
- [x] P2 Show `messagebox.showwarning` if episodes entry is 0 or negative
- [x] P2 Show `messagebox.showwarning` if alpha or gamma are out of (0, 1]
- [x] P2 Handle `KeyboardInterrupt` in `src/main.py` gracefully
- [x] P2 Handle `config/env.json` missing at startup: show error and exit cleanly
- [x] P2 Handle `config/rl.json` missing at startup: show error and exit cleanly

---

## Phase 9 — Documentation & Final Submission

### 9.1 README.md

- [x] P1 Create `README.md` in project root
- [x] P1 Write **Project Title** section
- [x] P1 Write **Overview** (1 paragraph describing the project)
- [x] P1 Write **System Requirements** (Python 3.11+, `uv` installed)
- [x] P1 Write **Installation** section with step-by-step commands
- [x] P1 Step: `git clone` (or unzip) the project
- [x] P1 Step: `cd ex1`
- [x] P1 Step: `uv sync --all-extras`
- [x] P1 Write **Running the Application** section
- [x] P1 Command: `uv run python src/main.py`
- [x] P1 Write **Running Tests** section
- [x] P1 Command: `uv run pytest`
- [x] P1 Command: `uv run pytest --cov=src --cov-report=term-missing`
- [x] P1 Write **Linting** section
- [x] P1 Command: `uv run ruff check .`
- [x] P1 Write **Configuration Reference** section
- [x] P1 Document every key in `config/env.json` with type and description
- [x] P1 Document every key in `config/rl.json` with type and description
- [x] P1 Write **Usage Guide** section
- [x] P1 Describe: how to start training
- [x] P1 Describe: how to pause and resume
- [x] P1 Describe: how to edit the grid
- [x] P1 Describe: how to save/load Q-Tables
- [x] P1 Describe: how to run a greedy evaluation episode
- [x] P1 Write **Project Structure** section (directory tree)
- [x] P1 Write **License** section
- [x] P1 Write **Author** section (course, student name, instructor)
- [x] P2 Add at least one screenshot of the running application
- [x] P2 Write **Troubleshooting** section (common errors + solutions)
- [x] P2 Write **Contributing** section (code style, linting, TDD)

### 9.2 Per-Algorithm PRD (Q-Learning)

- [x] P1 Create `docs/PRD_q_learning.md`
- [x] P1 Write **Algorithm Overview** — tabular Q-Learning, off-policy TD
- [x] P1 Write **Bellman Update Equation** with LaTeX-style notation
- [x] P1 Write **Hyperparameter Descriptions** table
- [x] P1 Describe α (alpha): effect on learning speed vs stability
- [x] P1 Describe γ (gamma): effect on long-term vs short-term focus
- [x] P1 Describe ε (epsilon): exploration vs exploitation trade-off
- [x] P1 Describe epsilon decay: why it's needed, how to tune
- [x] P1 Write **ε-Greedy Policy** section with pseudocode
- [x] P1 Write **Convergence Conditions** section
- [x] P1 Write **Limitations** section (tabular — no function approximation)
- [x] P1 Write **Design Decisions** section (why tabular, why off-policy)
- [x] P1 Write **Success Criteria** section (KPIs from PRD §4.2)
- [x] P1 Write **Specific Test Scenarios** for Q-Learning algorithm

### 9.3 Final Submission Checklist (Dr. Segal §17)

#### 9.3.1 Documentation (§17.1)

- [x] P1 `README.md` exists at project root and is comprehensive
- [x] P1 `docs/PRD.md` exists and is complete
- [x] P1 `docs/PLAN.md` exists and is complete
- [x] P1 `docs/TODO.md` exists (this file)
- [x] P1 `docs/PRD_q_learning.md` exists (per-algorithm PRD)
- [x] P1 Architecture diagrams (ASCII in PLAN.md) are accurate

#### 9.3.2 Architecture (§17.2)

- [x] P1 All business logic is accessible via SDK layer
- [x] P1 No RL or env logic in GUI modules
- [x] P1 OOP used throughout; no code duplication
- [x] P1 Mixins used for cross-cutting concerns (DroneSprite, GridEditor)
- [x] P1 All external calls go through SDK (no direct core imports in GUI)
- [x] P1 Rate-limit config exists (even if empty — `config/rl.json` serves this purpose)
- [x] P1 All source files ≤ 150 lines
- [x] P1 Docstrings present on all classes and public methods
- [x] P1 Consistent naming style across entire codebase

#### 9.3.3 Testing & Quality (§17.3)

- [x] P1 TDD followed: tests written before/with code for every module
- [x] P1 Coverage ≥ 85% (`uv run pytest --cov=src --cov-fail-under=85`)
- [x] P1 Ruff reports 0 errors (`uv run ruff check .`)
- [x] P1 Edge cases documented and tested
- [x] P1 Automated test report available (`pytest --html` or `term-missing`)

#### 9.3.4 Configuration & Security (§17.4)

- [x] P1 Config files separate from source code with version fields
- [x] P1 `.env-example` committed with placeholder values
- [x] P1 No API keys, passwords, or secrets in source code
- [x] P1 `.gitignore` is up to date
- [x] P1 `uv` used exclusively as package manager
- [x] P1 `pyproject.toml` and `uv.lock` both committed

#### 9.3.5 Versioning

- [x] P1 `version = "1.00"` set in `pyproject.toml`
- [x] P1 `__version__ = "1.00"` set in `src/core/version.py`
- [x] P1 Version displayed in application title bar
- [x] P1 Config files include `"version": "1.00"` field

#### 9.3.6 Standards Compliance (ISO/IEC 25010)

- [x] P3 Functional Suitability: all FR-* requirements implemented
- [x] P3 Performance Efficiency: 1000 episodes in < 60 s
- [x] P3 Usability: Nielsen's 10 heuristics satisfied (§8.14)
- [x] P3 Reliability: graceful error handling, no crashes on bad input
- [x] P3 Maintainability: modular, ≤150 lines/file, DRY, OOP
- [x] P3 Security: no hardcoded secrets, env-based config
- [x] P3 Portability: runs on Python 3.11+ on Linux/macOS/Windows

---

## Ongoing — Cross-Cutting Tasks

### Continuous Integration (Local)

- [x] P1 After EVERY code change: run `uv run ruff check .`
- [x] P1 After EVERY code change: run `uv run pytest`
- [x] P1 After EVERY new module: check file is ≤ 150 lines
- [x] P1 After EVERY new test: confirm test runs RED before implementation

### Git Discipline

- [x] P2 Commit after each phase completion gate passes
- [x] P2 Write meaningful commit messages (imperative mood, explains WHY)
- [x] P2 Tag version `v1.00` after Phase 8 completion gate passes
- [x] P2 Never commit `.env` file
- [x] P2 Never commit `*.pyc` or `__pycache__`

### Prompt Engineering Log

- [x] P3 Document each significant AI prompt used during development
- [x] P3 Record: context given, prompt text, output received
- [x] P3 Record: iterative refinements and why they were needed
- [x] P3 Note best practices discovered during vibe-coding process
- [x] P3 Save prompt log as `docs/PROMPT_LOG.md`

---

## Phase 10 — Unit Test Assignments

> Each item below is a **specific test function** to write in the corresponding test file.  
> Naming convention: `test_<subject>_<scenario>_<expected_outcome>`  
> File: `tests/core/test_<module>.py` or `tests/sdk/test_<module>.py`

---

### 10.1 `tests/core/test_cell_type.py`

- [x] P1 `test_cell_type_empty_exists` — `CellType.EMPTY` is a member of the enum
- [x] P1 `test_cell_type_building_exists` — `CellType.BUILDING` is a member of the enum
- [x] P1 `test_cell_type_trap_exists` — `CellType.TRAP` is a member of the enum
- [x] P1 `test_cell_type_wind_exists` — `CellType.WIND` is a member of the enum
- [x] P1 `test_cell_type_target_exists` — `CellType.TARGET` is a member of the enum
- [x] P1 `test_cell_type_start_exists` — `CellType.START` is a member of the enum
- [x] P1 `test_cell_type_has_six_members` — `len(CellType)` equals `6`
- [x] P1 `test_cell_type_all_values_unique` — all enum values are distinct
- [x] P1 `test_cell_type_is_enum_subclass` — `CellType` is a subclass of `enum.Enum`
- [x] P1 `test_cell_type_iteration_yields_all_members` — iterating yields exactly 6 items
- [x] P1 `test_cell_type_lookup_by_name_empty` — `CellType["EMPTY"]` returns `CellType.EMPTY`
- [x] P1 `test_cell_type_lookup_by_name_building` — `CellType["BUILDING"]` returns `CellType.BUILDING`
- [x] P1 `test_cell_type_lookup_by_name_trap` — `CellType["TRAP"]` returns `CellType.TRAP`
- [x] P1 `test_cell_type_lookup_by_name_wind` — `CellType["WIND"]` returns `CellType.WIND`
- [x] P1 `test_cell_type_lookup_by_name_target` — `CellType["TARGET"]` returns `CellType.TARGET`
- [x] P1 `test_cell_type_lookup_by_name_start` — `CellType["START"]` returns `CellType.START`
- [x] P1 `test_cell_type_invalid_name_raises_key_error` — `CellType["INVALID"]` raises `KeyError`
- [x] P1 `test_cell_type_members_not_equal_to_each_other` — `CellType.EMPTY != CellType.TRAP`
- [x] P1 `test_cell_type_same_member_is_equal_to_itself` — `CellType.TARGET == CellType.TARGET`
- [x] P1 `test_cell_type_name_attribute_matches_string` — `CellType.TRAP.name == "TRAP"`

---

### 10.2 `tests/core/test_config_loader.py`

- [x] P1 `test_load_env_config_returns_dict` — return type is `dict`
- [x] P1 `test_load_env_config_has_rows_key` — `"rows"` key exists in result
- [x] P1 `test_load_env_config_has_cols_key` — `"cols"` key exists in result
- [x] P1 `test_load_env_config_has_rewards_key` — `"rewards"` key exists in result
- [x] P1 `test_load_env_config_has_max_steps_key` — `"max_steps_per_episode"` key exists
- [x] P1 `test_load_env_config_has_default_grid_key` — `"default_grid"` key exists
- [x] P1 `test_load_env_config_rows_is_int` — `config["rows"]` is `int`
- [x] P1 `test_load_env_config_cols_is_int` — `config["cols"]` is `int`
- [x] P1 `test_load_env_config_rewards_is_dict` — `config["rewards"]` is `dict`
- [x] P1 `test_load_env_config_rewards_has_empty_key` — `config["rewards"]["empty"]` exists
- [x] P1 `test_load_env_config_rewards_has_wind_key` — `config["rewards"]["wind"]` exists
- [x] P1 `test_load_env_config_rewards_has_trap_key` — `config["rewards"]["trap"]` exists
- [x] P1 `test_load_env_config_rewards_has_building_key` — `config["rewards"]["building"]` exists
- [x] P1 `test_load_env_config_rewards_has_target_key` — `config["rewards"]["target"]` exists
- [x] P1 `test_load_env_config_default_grid_is_list` — `config["default_grid"]` is `list`
- [x] P1 `test_load_rl_config_returns_dict` — return type is `dict`
- [x] P1 `test_load_rl_config_has_alpha_key` — `"alpha"` key exists
- [x] P1 `test_load_rl_config_has_gamma_key` — `"gamma"` key exists
- [x] P1 `test_load_rl_config_has_epsilon_key` — `"epsilon"` key exists
- [x] P1 `test_load_rl_config_has_epsilon_decay_key` — `"epsilon_decay"` key exists
- [x] P1 `test_load_rl_config_has_epsilon_min_key` — `"epsilon_min"` key exists
- [x] P1 `test_load_rl_config_has_episodes_key` — `"episodes"` key exists
- [x] P1 `test_load_rl_config_has_max_steps_key` — `"max_steps"` key exists
- [x] P1 `test_load_rl_config_has_vis_every_n_key` — `"vis_every_n"` key exists
- [x] P1 `test_load_rl_config_alpha_is_float` — `config["alpha"]` is `float`
- [x] P1 `test_load_rl_config_gamma_in_range` — `0 < config["gamma"] <= 1`
- [x] P1 `test_load_rl_config_epsilon_in_range` — `0 <= config["epsilon"] <= 1`
- [x] P1 `test_load_env_config_missing_file_raises_file_not_found` — non-existent path raises `FileNotFoundError`
- [x] P1 `test_load_rl_config_missing_file_raises_file_not_found` — non-existent path raises `FileNotFoundError`
- [x] P1 `test_load_env_config_malformed_json_raises_json_decode_error` — bad JSON raises `json.JSONDecodeError`
- [x] P1 `test_load_rl_config_malformed_json_raises_json_decode_error` — bad JSON raises `json.JSONDecodeError`
- [x] P1 `test_load_env_config_custom_path_used` — passing a custom path reads from that file
- [x] P1 `test_load_rl_config_custom_path_used` — passing a custom path reads from that file
- [x] P2 `test_load_env_config_extra_keys_ignored` — unknown keys in JSON do not raise errors
- [x] P2 `test_load_rl_config_extra_keys_ignored` — unknown keys in JSON do not raise errors

---

### 10.3 `tests/core/test_episode_stats.py`

- [x] P1 `test_episode_stats_is_dataclass` — has `__dataclass_fields__` attribute
- [x] P1 `test_episode_stats_construction_all_fields` — construct with all five fields, no error
- [x] P1 `test_episode_stats_field_episode_type_is_int` — `stats.episode` is `int`
- [x] P1 `test_episode_stats_field_steps_type_is_int` — `stats.steps` is `int`
- [x] P1 `test_episode_stats_field_total_reward_type_is_float` — `stats.total_reward` is `float`
- [x] P1 `test_episode_stats_field_max_delta_q_type_is_float` — `stats.max_delta_q` is `float`
- [x] P1 `test_episode_stats_field_reached_target_type_is_bool` — `stats.reached_target` is `bool`
- [x] P1 `test_episode_stats_equality_same_values` — two instances with same values are equal
- [x] P1 `test_episode_stats_inequality_different_episode` — different `episode` values are not equal
- [x] P1 `test_episode_stats_inequality_different_reward` — different `total_reward` values are not equal
- [x] P1 `test_episode_stats_reached_target_true` — `reached_target=True` stores correctly
- [x] P1 `test_episode_stats_reached_target_false` — `reached_target=False` stores correctly
- [x] P1 `test_episode_stats_negative_reward_stored` — negative `total_reward` stores correctly
- [x] P1 `test_episode_stats_zero_steps_valid` — `steps=0` is a valid value
- [x] P1 `test_episode_stats_zero_delta_q_valid` — `max_delta_q=0.0` is a valid value
- [x] P2 `test_episode_stats_repr_contains_episode` — `repr(stats)` contains episode number
- [x] P2 `test_episode_stats_fields_list_has_five_items` — `len(dataclasses.fields(EpisodeStats)) == 5`

---

### 10.4 `tests/core/test_grid_world.py`

#### Construction

- [x] P1 `test_grid_world_init_no_error` — `GridWorld(config)` constructs without exception
- [x] P1 `test_grid_world_num_states_equals_rows_times_cols` — `num_states == rows * cols`
- [x] P1 `test_grid_world_num_actions_equals_four` — `num_actions == 4`
- [x] P1 `test_grid_world_get_grid_returns_2d_list` — `get_grid()` is a list of lists
- [x] P1 `test_grid_world_get_grid_row_count` — outer list has `rows` items
- [x] P1 `test_grid_world_get_grid_col_count` — each inner list has `cols` items
- [x] P1 `test_grid_world_get_grid_all_cell_types` — every element is a `CellType`
- [x] P1 `test_grid_world_exactly_one_start_cell` — exactly one cell is `CellType.START`
- [x] P1 `test_grid_world_exactly_one_target_cell` — exactly one cell is `CellType.TARGET`
- [x] P1 `test_grid_world_small_grid_5x5_num_states` — `5×5` config yields `num_states=25`
- [x] P1 `test_grid_world_small_grid_3x3_num_states` — `3×3` config yields `num_states=9`

#### `reset()`

- [x] P1 `test_grid_world_reset_returns_int` — `reset()` returns `int`
- [x] P1 `test_grid_world_reset_returns_start_state` — returned state corresponds to `START` cell
- [x] P1 `test_grid_world_reset_idempotent` — two calls return the same value
- [x] P1 `test_grid_world_reset_state_in_valid_range` — `0 <= reset() < num_states`

#### `step()` — Return Types

- [x] P1 `test_grid_world_step_returns_tuple_of_three` — `step()` returns 3-tuple
- [x] P1 `test_grid_world_step_next_state_is_int` — first element is `int`
- [x] P1 `test_grid_world_step_reward_is_float` — second element is `float`
- [x] P1 `test_grid_world_step_done_is_bool` — third element is `bool`
- [x] P1 `test_grid_world_step_next_state_in_range` — `0 <= next_state < num_states`

#### `step()` — Directional Movement

- [x] P1 `test_grid_world_step_up_decrements_row` — action UP moves to row-1 when valid
- [x] P1 `test_grid_world_step_down_increments_row` — action DOWN moves to row+1 when valid
- [x] P1 `test_grid_world_step_left_decrements_col` — action LEFT moves to col-1 when valid
- [x] P1 `test_grid_world_step_right_increments_col` — action RIGHT moves to col+1 when valid

#### `step()` — Cell Rewards

- [x] P1 `test_grid_world_step_into_empty_reward_minus_one` — stepping into `EMPTY` gives `-1.0`
- [x] P1 `test_grid_world_step_into_empty_done_false` — stepping into `EMPTY` gives `done=False`
- [x] P1 `test_grid_world_step_into_trap_reward_minus_ten` — stepping into `TRAP` gives `-10.0`
- [x] P1 `test_grid_world_step_into_trap_done_false` — stepping into `TRAP` gives `done=False`
- [x] P1 `test_grid_world_step_into_wind_reward_minus_three` — stepping into `WIND` gives `-3.0`
- [x] P1 `test_grid_world_step_into_wind_done_false` — stepping into `WIND` gives `done=False`
- [x] P1 `test_grid_world_step_into_target_reward_plus_fifty` — stepping into `TARGET` gives `+50.0`
- [x] P1 `test_grid_world_step_into_target_done_true` — stepping into `TARGET` gives `done=True`
- [x] P1 `test_grid_world_step_into_target_next_state_is_target_index` — next_state equals target index

#### `step()` — Rewards from Config

- [x] P1 `test_grid_world_step_trap_reward_from_config` — reward value matches `config["rewards"]["trap"]`
- [x] P1 `test_grid_world_step_wind_reward_from_config` — reward value matches `config["rewards"]["wind"]`
- [x] P1 `test_grid_world_step_target_reward_from_config` — reward value matches `config["rewards"]["target"]`
- [x] P1 `test_grid_world_step_empty_reward_from_config` — reward value matches `config["rewards"]["empty"]`

#### `step()` — Boundary & Building

- [x] P1 `test_grid_world_step_up_from_top_row_stays_same` — UP from row 0 → same state
- [x] P1 `test_grid_world_step_down_from_bottom_row_stays_same` — DOWN from last row → same state
- [x] P1 `test_grid_world_step_left_from_leftmost_col_stays_same` — LEFT from col 0 → same state
- [x] P1 `test_grid_world_step_right_from_rightmost_col_stays_same` — RIGHT from last col → same state
- [x] P1 `test_grid_world_step_boundary_reward_is_minus_one` — boundary collision gives `-1.0`
- [x] P1 `test_grid_world_step_boundary_done_false` — boundary collision gives `done=False`
- [x] P1 `test_grid_world_step_into_building_stays_same_state` — building collision → same state
- [x] P1 `test_grid_world_step_into_building_reward_minus_one` — building collision gives `-1.0`
- [x] P1 `test_grid_world_step_into_building_done_false` — building collision gives `done=False`

#### `step()` — Max Steps

- [x] P1 `test_grid_world_step_max_steps_exceeded_done_true` — after `max_steps` steps, `done=True`
- [x] P1 `test_grid_world_step_max_steps_from_config` — max_steps limit uses config value
- [x] P1 `test_grid_world_step_before_max_steps_done_false` — one step before limit, `done=False`

#### State ↔ Position

- [x] P1 `test_grid_world_pos_to_state_origin` — `pos_to_state(0, 0) == 0`
- [x] P1 `test_grid_world_pos_to_state_formula` — `pos_to_state(r, c) == r * cols + c`
- [x] P1 `test_grid_world_state_to_pos_origin` — `state_to_pos(0) == (0, 0)`
- [x] P1 `test_grid_world_state_to_pos_last_cell` — `state_to_pos(num_states-1) == (rows-1, cols-1)`
- [x] P1 `test_grid_world_roundtrip_pos_to_state_to_pos` — `state_to_pos(pos_to_state(r,c)) == (r,c)`
- [x] P1 `test_grid_world_roundtrip_state_to_pos_to_state` — `pos_to_state(*state_to_pos(s)) == s`

#### Grid Mutation

- [x] P1 `test_grid_world_set_cell_changes_cell_type` — after `set_cell(r,c,TRAP)`, `get_grid()[r][c] == TRAP`
- [x] P1 `test_grid_world_set_cell_only_changes_target_cell` — other cells unchanged after `set_cell`
- [x] P1 `test_grid_world_get_grid_returns_independent_copy` — mutating returned list does not affect internal grid

---

### 10.5 `tests/core/test_q_agent.py`

#### Construction

- [x] P1 `test_q_agent_init_no_error` — `QLearningAgent(n_s, n_a, config)` constructs without error
- [x] P1 `test_q_agent_q_table_is_numpy_array` — `get_q_table()` returns `np.ndarray`
- [x] P1 `test_q_agent_q_table_shape_num_states_x_4` — shape is `(num_states, 4)`
- [x] P1 `test_q_agent_q_table_initialized_to_zeros` — all values equal `0.0`
- [x] P1 `test_q_agent_get_policy_returns_dict` — `get_policy()` returns `dict`
- [x] P1 `test_q_agent_get_policy_has_all_states` — dict has `num_states` entries
- [x] P1 `test_q_agent_get_policy_all_actions_valid` — every value is in `{0, 1, 2, 3}`

#### Bellman Update

- [x] P1 `test_q_agent_update_returns_float` — `update()` returns `float`
- [x] P1 `test_q_agent_update_zero_table_terminal_step` — `Q(s,a) == alpha * r` when `max Q(s')=0`
- [x] P1 `test_q_agent_update_positive_reward_increases_q` — positive reward increases `Q(s,a)`
- [x] P1 `test_q_agent_update_negative_reward_decreases_q` — negative reward decreases `Q(s,a)`
- [x] P1 `test_q_agent_update_exact_bellman_formula` — numeric assertion with known alpha/gamma/r/Q values
- [x] P1 `test_q_agent_update_delta_q_equals_absolute_change` — returned Δq equals `|new - old|`
- [x] P1 `test_q_agent_update_only_changes_given_action` — other `(s, a')` entries unchanged
- [x] P1 `test_q_agent_update_uses_max_next_q` — uses `max Q(s', a')` over all actions
- [x] P1 `test_q_agent_update_alpha_zero_no_change` — with `alpha=0`, Q-Table unchanged
- [x] P1 `test_q_agent_update_alpha_one_full_update` — with `alpha=1`, `Q(s,a) = r + gamma * max_Q`
- [x] P1 `test_q_agent_update_gamma_zero_ignores_future` — with `gamma=0`, `Q(s,a) += alpha*(r - Q(s,a))`
- [x] P1 `test_q_agent_update_multiple_steps_accumulates` — Q(s,a) after 5 updates is consistent with formula

#### ε-Greedy Action Selection

- [x] P1 `test_q_agent_select_action_returns_int` — `select_action(state)` returns `int`
- [x] P1 `test_q_agent_select_action_in_valid_range` — returned value in `[0, num_actions)`
- [x] P1 `test_q_agent_select_action_epsilon_one_is_random` — with `epsilon=1.0`, 100 calls not always same action (probabilistic)
- [x] P1 `test_q_agent_select_action_epsilon_zero_is_greedy` — with `epsilon=0.0`, always returns `argmax Q(s,a)`
- [x] P1 `test_q_agent_select_action_greedy_picks_max_q` — manually set one Q high, greedy picks it
- [x] P1 `test_q_agent_select_action_zero_q_table_valid_action` — all-zero table still returns valid action
- [x] P1 `test_q_agent_select_action_different_states_different_best` — each state returns its own argmax
- [x] P1 `test_q_agent_select_action_epsilon_half_distribution` — with `epsilon=0.5`, roughly 50% random (statistical test)

#### Epsilon Decay

- [x] P1 `test_q_agent_end_episode_returns_episode_stats` — `end_episode()` returns `EpisodeStats`
- [x] P1 `test_q_agent_end_episode_epsilon_decreases` — epsilon after `end_episode()` < epsilon before
- [x] P1 `test_q_agent_end_episode_epsilon_decay_multiplied` — `new_eps == max(old_eps * decay, eps_min)`
- [x] P1 `test_q_agent_end_episode_epsilon_not_below_min` — epsilon never goes below `epsilon_min`
- [x] P1 `test_q_agent_end_episode_epsilon_at_min_stays_at_min` — starting at `epsilon_min`, stays at `epsilon_min`
- [x] P1 `test_q_agent_end_episode_increments_episode_counter` — episode number increments by 1
- [x] P1 `test_q_agent_end_episode_stats_episode_matches_counter` — `stats.episode` matches internal counter
- [x] P1 `test_q_agent_end_episode_stats_steps_reflects_counted_steps` — `stats.steps` matches steps counted
- [x] P1 `test_q_agent_end_episode_stats_total_reward_reflects_cumulative` — `stats.total_reward` matches sum
- [x] P1 `test_q_agent_end_episode_stats_max_delta_q_is_max_of_episode` — `stats.max_delta_q` equals max Δq
- [x] P1 `test_q_agent_end_episode_resets_per_episode_accumulators` — after `end_episode`, steps=0, reward=0
- [x] P1 `test_q_agent_end_episode_reached_target_true_when_signaled` — `reached_target` reflects signal
- [x] P1 `test_q_agent_end_episode_reached_target_false_by_default` — without signal, `reached_target=False`

#### Reset

- [x] P1 `test_q_agent_reset_q_table_all_zeros` — after `reset()`, all Q values are `0.0`
- [x] P1 `test_q_agent_reset_episode_counter_to_zero` — after `reset()`, episode counter is `0`
- [x] P1 `test_q_agent_reset_epsilon_to_initial_value` — after `reset()`, epsilon equals config `epsilon`
- [x] P1 `test_q_agent_reset_after_training_clears_q_table` — Q-Table previously non-zero is zeroed

#### Hyperparameter Update

- [x] P1 `test_q_agent_update_params_alpha_changes` — `update_params(alpha=0.5)` changes alpha to `0.5`
- [x] P1 `test_q_agent_update_params_gamma_changes` — `update_params(gamma=0.8)` changes gamma to `0.8`
- [x] P1 `test_q_agent_update_params_epsilon_changes` — `update_params(epsilon=0.3)` changes epsilon to `0.3`
- [x] P1 `test_q_agent_update_params_unknown_key_raises_value_error` — unknown key raises `ValueError`
- [x] P1 `test_q_agent_update_params_multiple_keys_at_once` — can update alpha and gamma simultaneously
- [x] P1 `test_q_agent_update_params_effect_on_next_update` — changed alpha affects next Bellman update

#### Q-Table Set/Get

- [x] P1 `test_q_agent_set_q_table_replaces_internal_table` — `get_q_table()` returns new table after `set_q_table()`
- [x] P1 `test_q_agent_set_q_table_wrong_shape_raises_value_error` — wrong shape raises `ValueError`
- [x] P1 `test_q_agent_set_q_table_stores_independent_copy` — modifying external array after set does not affect agent
- [x] P1 `test_q_agent_get_q_table_returns_copy` — modifying returned array does not affect agent

---

### 10.6 `tests/sdk/test_persistence.py`

#### Q-Table Save / Load

- [x] P1 `test_save_q_table_creates_file` — file exists after `save_q_table(q, path)`
- [x] P1 `test_save_q_table_file_has_npy_extension` — saved file ends in `.npy`
- [x] P1 `test_load_q_table_returns_ndarray` — `load_q_table(path)` returns `np.ndarray`
- [x] P1 `test_save_load_q_table_roundtrip_shape` — loaded array has same shape as saved
- [x] P1 `test_save_load_q_table_roundtrip_values` — `np.allclose(original, loaded)` is True
- [x] P1 `test_save_load_q_table_roundtrip_10x4` — specific shape `(10, 4)` round-trips correctly
- [x] P1 `test_save_load_q_table_roundtrip_100x4` — shape `(100, 4)` round-trips correctly
- [x] P1 `test_save_load_q_table_nonzero_values` — non-zero values preserved exactly
- [x] P1 `test_save_load_q_table_negative_values` — negative values preserved exactly
- [x] P1 `test_load_q_table_nonexistent_path_raises_file_not_found` — `FileNotFoundError` on bad path
- [x] P1 `test_save_q_table_overwrites_existing_file` — saving twice to same path overwrites cleanly
- [x] P1 `test_save_q_table_all_zeros_table` — all-zero table round-trips correctly
- [x] P2 `test_save_load_q_table_large_table` — shape `(10000, 4)` round-trips in < 1 second

#### Grid Save / Load

- [x] P1 `test_save_grid_creates_file` — file exists after `save_grid(grid, path)`
- [x] P1 `test_save_grid_produces_valid_json` — saved file is parseable by `json.loads`
- [x] P1 `test_load_grid_returns_list_of_lists` — `load_grid(path)` returns `list[list[...]]`
- [x] P1 `test_load_grid_elements_are_cell_types` — every element is a `CellType`
- [x] P1 `test_save_load_grid_roundtrip_empty_cell` — `CellType.EMPTY` round-trips
- [x] P1 `test_save_load_grid_roundtrip_building_cell` — `CellType.BUILDING` round-trips
- [x] P1 `test_save_load_grid_roundtrip_trap_cell` — `CellType.TRAP` round-trips
- [x] P1 `test_save_load_grid_roundtrip_wind_cell` — `CellType.WIND` round-trips
- [x] P1 `test_save_load_grid_roundtrip_target_cell` — `CellType.TARGET` round-trips
- [x] P1 `test_save_load_grid_roundtrip_start_cell` — `CellType.START` round-trips
- [x] P1 `test_save_load_grid_roundtrip_full_3x3_grid` — full 3×3 grid with mixed types round-trips
- [x] P1 `test_save_load_grid_row_count_preserved` — row count unchanged after round-trip
- [x] P1 `test_save_load_grid_col_count_preserved` — column count unchanged after round-trip
- [x] P1 `test_load_grid_nonexistent_path_raises_file_not_found` — `FileNotFoundError` on bad path

---

### 10.7 `tests/sdk/test_training_loop.py`

- [x] P1 `test_training_loop_init_no_error` — `TrainingLoop(env, agent, queue, config)` constructs without error
- [x] P1 `test_training_loop_start_creates_thread` — after `start()`, `loop._thread` is not None
- [x] P1 `test_training_loop_start_thread_is_daemon` — thread is a daemon thread
- [x] P1 `test_training_loop_start_thread_is_alive` — thread is alive shortly after `start()`
- [x] P1 `test_training_loop_queue_receives_stats_within_timeout` — at least one `EpisodeStats` posted to queue within 5s
- [x] P1 `test_training_loop_stats_is_episode_stats_instance` — item from queue is `EpisodeStats`
- [x] P1 `test_training_loop_pause_stops_new_stats` — after `pause()`, no new stats in 1 second
- [x] P1 `test_training_loop_resume_after_pause_restarts_stats` — after `resume()`, stats flow again within 3s
- [x] P1 `test_training_loop_stop_terminates_thread` — after `stop()`, thread not alive within 3s
- [x] P1 `test_training_loop_stop_before_start_no_error` — calling `stop()` without `start()` raises no exception
- [x] P1 `test_training_loop_episode_counter_increases` — second stat has higher episode number than first
- [x] P1 `test_training_loop_pause_is_idempotent` — calling `pause()` twice does not raise
- [x] P1 `test_training_loop_resume_without_pause_no_error` — calling `resume()` without prior `pause()` is safe
- [x] P2 `test_training_loop_stop_clears_queue_cleanly` — queue is not corrupted after stop

---

### 10.8 `tests/sdk/test_drone_sim_sdk.py`

#### Construction

- [x] P1 `test_sdk_init_no_error` — `DroneSimSDK()` constructs without error
- [x] P1 `test_sdk_get_stats_queue_returns_queue` — `get_stats_queue()` returns `queue.Queue`
- [x] P1 `test_sdk_get_q_table_returns_ndarray` — `get_q_table()` returns `np.ndarray`
- [x] P1 `test_sdk_get_q_table_initially_all_zeros` — freshly constructed SDK has all-zero Q-Table
- [x] P1 `test_sdk_get_policy_returns_dict` — `get_policy()` returns `dict`
- [x] P1 `test_sdk_get_policy_all_actions_valid` — all values in policy dict are in `{0,1,2,3}`

#### Training Lifecycle

- [x] P1 `test_sdk_start_training_no_error` — `start_training()` does not raise
- [x] P1 `test_sdk_start_training_queue_receives_stats` — queue has at least 1 stat within 10s
- [x] P1 `test_sdk_start_training_twice_no_double_thread` — second call does not spawn extra thread
- [x] P1 `test_sdk_pause_no_error` — `pause()` does not raise
- [x] P1 `test_sdk_resume_no_error` — `resume()` does not raise
- [x] P1 `test_sdk_stop_no_error` — `stop()` does not raise
- [x] P1 `test_sdk_stop_before_start_no_error` — `stop()` without prior `start_training()` is safe
- [x] P1 `test_sdk_stop_then_start_resumes_training` — can call `start_training()` after `stop()`

#### Reset

- [x] P1 `test_sdk_reset_q_table_to_zeros` — after `reset()`, `get_q_table()` is all zeros
- [x] P1 `test_sdk_reset_after_training_clears_table` — Q-Table was non-zero, reset zeros it
- [x] P1 `test_sdk_reset_episode_counter_to_zero` — stats after reset show episode `1`

#### Evaluate

- [x] P1 `test_sdk_evaluate_returns_tuple` — `evaluate()` returns a 2-tuple
- [x] P1 `test_sdk_evaluate_path_is_list` — first element is `list`
- [x] P1 `test_sdk_evaluate_stats_is_episode_stats` — second element is `EpisodeStats`
- [x] P1 `test_sdk_evaluate_path_contains_ints` — all elements of path are `int`
- [x] P1 `test_sdk_evaluate_path_states_in_valid_range` — all path states in `[0, num_states)`
- [x] P1 `test_sdk_evaluate_does_not_modify_q_table` — Q-Table unchanged before and after `evaluate()`
- [x] P1 `test_sdk_evaluate_with_all_zero_q_table_returns_valid_path` — works even with zero Q-Table
- [x] P1 `test_sdk_evaluate_path_starts_at_start_state` — first element of path equals `reset()` state

#### Hyperparams & Grid Update

- [x] P1 `test_sdk_set_hyperparams_alpha_changes` — `set_hyperparams(alpha=0.5)` takes effect
- [x] P1 `test_sdk_set_hyperparams_gamma_changes` — `set_hyperparams(gamma=0.8)` takes effect
- [x] P1 `test_sdk_set_hyperparams_epsilon_changes` — `set_hyperparams(epsilon=0.1)` takes effect
- [x] P1 `test_sdk_update_grid_changes_cell_type` — `update_grid(r, c, CellType.TRAP)` reflects in grid
- [x] P1 `test_sdk_update_grid_invalid_row_raises` — out-of-bounds row raises `IndexError` or `ValueError`
- [x] P1 `test_sdk_update_grid_invalid_col_raises` — out-of-bounds col raises `IndexError` or `ValueError`

#### Persistence via SDK

- [x] P1 `test_sdk_save_q_table_creates_file` — `save_q_table(path)` writes file at path
- [x] P1 `test_sdk_load_q_table_restores_table` — loaded table matches previously saved table
- [x] P1 `test_sdk_load_q_table_nonexistent_path_raises` — `FileNotFoundError` propagated to caller

---

### 10.9 `tests/core/test_version.py`

- [x] P1 `test_version_module_has_version_attribute` — `version.__version__` exists
- [x] P1 `test_version_is_string` — `__version__` is a `str`
- [x] P1 `test_version_matches_pyproject` — `__version__` value equals `"1.00"`
- [x] P1 `test_version_format_x_dot_xx` — matches pattern `\d+\.\d{2}`
- [x] P2 `test_version_importable_from_core_init` — `from src.core import __version__` works

---

### 10.10 `tests/conftest.py` — Shared Fixtures

- [x] P1 Define fixture `env_config` — returns default env config dict (used across multiple test files)
- [x] P1 Define fixture `rl_config` — returns default RL config dict
- [x] P1 Define fixture `small_env_config` — 3×3 grid config for fast tests
- [x] P1 Define fixture `grid_world` — `GridWorld` instance using `env_config`
- [x] P1 Define fixture `small_grid_world` — `GridWorld` instance using `small_env_config`
- [x] P1 Define fixture `q_agent` — `QLearningAgent` instance using `rl_config` and grid dimensions
- [x] P1 Define fixture `tmp_npy_path` — temporary `.npy` file path (auto-cleanup)
- [x] P1 Define fixture `tmp_json_path` — temporary `.json` file path (auto-cleanup)
- [x] P1 Define fixture `sample_q_table_10x4` — `np.random.rand(10, 4)` with fixed seed
- [x] P1 Define fixture `sample_3x3_grid` — 2-D list of `CellType` values for a 3×3 grid
- [x] P1 Define fixture `sdk_instance` — `DroneSimSDK()` instance (fresh, not training)
- [x] P2 Define fixture `trained_sdk` — SDK that has completed 10 episodes (for evaluate tests)

---

### 10.11 Test File Structure & Compliance

- [x] P1 Verify `tests/core/test_cell_type.py` ≤ 150 lines
- [x] P1 Verify `tests/core/test_config_loader.py` ≤ 150 lines
- [x] P1 Verify `tests/core/test_episode_stats.py` ≤ 150 lines
- [x] P1 Verify `tests/core/test_grid_world.py` ≤ 150 lines — split if needed
- [x] P1 Verify `tests/core/test_q_agent.py` ≤ 150 lines — split into `test_q_agent_update.py` etc. if needed
- [x] P1 Verify `tests/sdk/test_persistence.py` ≤ 150 lines
- [x] P1 Verify `tests/sdk/test_training_loop.py` ≤ 150 lines
- [x] P1 Verify `tests/sdk/test_drone_sim_sdk.py` ≤ 150 lines — split if needed
- [x] P1 Verify `tests/core/test_version.py` ≤ 150 lines
- [x] P1 Verify `tests/conftest.py` ≤ 150 lines
- [x] P1 Run `uv run ruff check tests/` → 0 errors on all test files
- [x] P1 All test functions follow naming convention `test_<subject>_<scenario>_<expected>`
- [x] P1 All test functions have a docstring (one-line) explaining what is verified
- [x] P1 No test function uses a hardcoded reward value (use `config["rewards"][...]`)
- [x] P1 All tests that create temp files use `tmp_path` fixture (pytest built-in) for cleanup
- [x] P1 No test has a dependency on another test's execution order
- [x] P1 No test imports from `src.gui.*` (GUI not unit-testable without display)
- [x] P2 Each test file has a module-level docstring naming the module under test
- [x] P2 `conftest.py` has docstring explaining available fixtures

---

---

## Phase 11 — PRD Gap Completions

> These tasks were identified by auditing the PRD against all prior phases.  
> Each item is labelled with the PRD requirement ID it satisfies.

---

### 11.1 Version Field in Config Files (§9.3.5)

> **PRD §9.3.5** — "Config files include `"version": "1.00"` field."  
> Phase 1.6 creates both JSON files but never adds the `version` key.

- [x] P1 Add `"version": "1.00"` as the first key in `config/env.json`
- [x] P1 Add `"version": "1.00"` as the first key in `config/rl.json`
- [x] P1 Write test `test_load_env_config_has_version_key` — `config["version"]` exists in env config
- [x] P1 Write test `test_load_rl_config_has_version_key` — `config["version"]` exists in rl config
- [x] P1 Write test `test_load_env_config_version_is_string` — `config["version"]` is a `str`
- [x] P2 Write test `test_load_env_config_version_matches_app_version` — env config version matches `__version__`

---

### 11.2 Animation Step Delay in TrainingLoop (FR-GUI-DRONE-02, FR-GUI-CTRL-09)

> **FR-GUI-DRONE-02** — "During a live training episode the drone shall animate step-by-step."  
> **FR-GUI-CTRL-09** — "Speed slider — controls the visualisation delay between steps (range: instant → 500 ms/step)."  
> The speed slider widget exists in ControlPanel tasks but `TrainingLoop` has no `vis_delay` attribute and no implementation task for sleeping between steps.

- [x] P2 Add `vis_delay: float` attribute to `TrainingLoop.__init__` (default `0.0` seconds)
- [x] P2 In `TrainingLoop._run()`, sleep `vis_delay` seconds after each step when `vis_delay > 0`
- [x] P2 Implement `set_vis_delay(self, ms: int) -> None` in `TrainingLoop` (converts ms → seconds)
- [x] P2 Add `set_vis_delay(self, ms: int) -> None` to `DroneSimSDK` (delegates to `TrainingLoop`)
- [x] P2 Speed slider `<Motion>` callback in `ControlPanel` calls `sdk.set_vis_delay(int(slider_value))`
- [x] P2 Speed slider initial value reads from `config/rl.json` (add `"vis_delay_ms": 0` to `rl.json`)
- [x] P2 Add `"vis_delay_ms": 0` key to `config/rl.json`
- [x] P2 Write RED test `test_training_loop_default_vis_delay_is_zero` — `loop.vis_delay == 0.0` after init
- [x] P2 Write RED test `test_training_loop_set_vis_delay_stores_seconds` — `set_vis_delay(100)` → `vis_delay == 0.1`
- [x] P2 Write RED test `test_training_loop_set_vis_delay_zero_no_sleep` — `set_vis_delay(0)` has no timing effect
- [x] P2 Write RED test `test_sdk_set_vis_delay_delegates_to_loop` — `sdk.set_vis_delay(200)` reflects in loop
- [x] P2 Verify `TrainingLoop` tests cover `set_vis_delay` — add to `tests/sdk/test_training_loop.py`
- [x] P2 Verify `TrainingLoop` still ≤ 150 lines after adding vis_delay logic

---

### 11.3 Scrollable / Zoomable Convergence Graph (FR-GUI-GRAPH-04)

> **FR-GUI-GRAPH-04** — "The graph shall be scrollable / zoomable if the number of episodes is large."  
> Phase 6 creates a matplotlib figure but never adds the navigation toolbar.

- [x] P2 Import `NavigationToolbar2Tk` from `matplotlib.backends.backend_tkagg` in `graph_panel.py`
- [x] P2 Instantiate `NavigationToolbar2Tk(canvas_widget, self)` below the canvas in `GraphPanel.__init__`
- [x] P2 Call `toolbar.update()` after instantiation
- [x] P2 Ensure toolbar is packed/gridded below the matplotlib canvas
- [x] P2 Verify toolbar provides: zoom, pan, home, back, forward, save buttons
- [x] P2 Test: `GraphPanel` widget tree contains a `NavigationToolbar2Tk` child
- [x] P2 Verify `graph_panel.py` ≤ 150 lines after toolbar addition; split if needed

---

### 11.4 Grid Reachability Check (PRD Assumption 3)

> **PRD Assumption 3** — "A path from Start to Target always exists (the grid editor shall warn if it does not)."  
> `GridEditorMixin` enforces the START/TARGET count constraint but never validates that a walkable path exists.

- [x] P2 Implement `_check_reachability(self) -> bool` in `GridEditorMixin` using BFS
  - BFS from the START cell over non-BUILDING cells
  - Returns `True` if TARGET cell is reachable; `False` otherwise
- [x] P2 Implement helper `_find_cell(self, cell_type: CellType) -> tuple[int, int] | None` — scans grid for first matching cell type
- [x] P2 After every `_paint_cell(row, col)` call, invoke `_check_reachability()`
- [x] P2 If `_check_reachability()` returns `False`: revert the painted cell to its previous type and call `show_edit_warning("No walkable path from Start to Target — edit reverted.")`
- [x] P2 Store previous cell type before painting so revert is possible
- [x] P2 Also call `_check_reachability()` when **Reset Grid** completes (safety check)
- [x] P2 Write test `test_check_reachability_open_grid_returns_true` — open 3×3 grid with START at (0,0), TARGET at (2,2) → True
- [x] P2 Write test `test_check_reachability_blocked_returns_false` — column of BUILDING cells separates START from TARGET → False
- [x] P2 Write test `test_check_reachability_diagonal_building_still_passable` — path around corner exists → True
- [x] P2 Write test `test_find_cell_returns_correct_position` — `_find_cell(CellType.START)` returns START coords
- [x] P2 Write test `test_find_cell_missing_type_returns_none` — type not present → `None`
- [x] P2 Verify `GridEditorMixin` ≤ 150 lines after reachability logic; extract `_bfs_reachable` if needed

---

### 11.5 `sdk.load_default_grid()` — SDK-Layer Tests (FR-GUI-GRID-06)

> **FR-GUI-GRID-06** — "Provide a reset grid button that restores the grid to the default layout loaded from `config/env.json`."  
> Phase 7 references `sdk.load_default_grid()` and implements it, but Phase 4 (SDK TDD) has no test coverage for this method.

- [x] P1 Write RED test `test_sdk_load_default_grid_method_exists` — `DroneSimSDK` has `load_default_grid` attribute
- [x] P1 Write RED test `test_sdk_load_default_grid_no_error` — calling it raises no exception
- [x] P1 Write RED test `test_sdk_load_default_grid_grid_has_one_start` — resulting grid has exactly one `CellType.START`
- [x] P1 Write RED test `test_sdk_load_default_grid_grid_has_one_target` — resulting grid has exactly one `CellType.TARGET`
- [x] P1 Write RED test `test_sdk_load_default_grid_matches_config_dimensions` — grid rows/cols match `config/env.json`
- [x] P2 Write RED test `test_sdk_load_default_grid_after_edit_restores_original` — after `update_grid(…)`, calling `load_default_grid()` restores original layout
- [x] P1 Add `test_sdk_load_default_grid_*` test functions to `tests/sdk/test_drone_sim_sdk.py`
- [x] P1 Confirm `test_drone_sim_sdk.py` ≤ 150 lines after additions; split into `test_drone_sim_sdk_grid.py` if needed

---

### 11.6 `vis_every_n` Wired into GridCanvas (FR-GUI-GRID-04)

> **FR-GUI-GRID-04** — "The grid shall update in real time as episodes progress. Update frequency is configurable (e.g., refresh every N episodes)."  
> `vis_every_n` is in `config/rl.json` and used in `GraphPanel.append()`, but `GridCanvas.refresh()` is called from `App._poll_queue` on every stat — the grid should also respect the configurable frequency.

- [x] P2 `App._poll_queue` reads `vis_every_n` from SDK config (or stores it at init time)
- [x] P2 `App._poll_queue` only calls `GridCanvas.refresh()` when `stats.episode % vis_every_n == 0`
- [x] P2 `App._poll_queue` always calls `Dashboard.update(stats)` (every episode — not throttled)
- [x] P2 `App._poll_queue` always calls `GraphPanel.append(stats)` (GraphPanel throttles internally)
- [x] P2 Add task: confirm `vis_every_n` is passed to `App` from SDK or read from config at `App.__init__`
- [x] P2 Write integration check: with `vis_every_n=5`, `GridCanvas.refresh` is called only on episodes divisible by 5

---

### 11.7 Module-Level Docstrings (NFR-DOC, §8.10)

> **NFR-DOC / §8.10** — "Verify every module has a module-level docstring."  
> Phase 8 audits for docstrings but no implementation tasks in Phases 2–4 explicitly require writing module-level docstrings.

- [x] P2 Add module-level docstring to `src/core/cell_type.py` — one sentence describing contents
- [x] P2 Add module-level docstring to `src/core/config_loader.py` — describe loader purpose
- [x] P2 Add module-level docstring to `src/core/episode_stats.py` — describe dataclass purpose
- [x] P2 Add module-level docstring to `src/core/grid_world.py` — describe grid environment role
- [x] P2 Add module-level docstring to `src/core/q_agent.py` — describe Q-Learning agent role
- [x] P2 Add module-level docstring to `src/core/version.py` — single-line version declaration
- [x] P2 Add module-level docstring to `src/sdk/drone_sim_sdk.py` — describe SDK façade pattern
- [x] P2 Add module-level docstring to `src/sdk/training_loop.py` — describe threading model
- [x] P2 Add module-level docstring to `src/sdk/persistence.py` — describe save/load utilities
- [x] P2 Add module-level docstring to `src/gui/app.py` — describe root window and layout
- [x] P2 Add module-level docstring to `src/gui/grid_canvas.py` — describe canvas widget role
- [x] P2 Add module-level docstring to `src/gui/drone_sprite.py` — describe mixin contract
- [x] P2 Add module-level docstring to `src/gui/grid_editor.py` — describe mixin contract
- [x] P2 Add module-level docstring to `src/gui/control_panel.py` — describe control panel role
- [x] P2 Add module-level docstring to `src/gui/dashboard.py` — describe stats display role
- [x] P2 Add module-level docstring to `src/gui/graph_panel.py` — describe graph widget role
- [x] P2 Add module-level docstring to `src/main.py` — entry-point description
- [x] P1 Verify all `tests/` files have module-level docstrings naming the module under test (see §10.11)

---

### 11.8 Additional Test Functions for New Features (Phase 10 Addenda)

> These test functions extend Phase 10 to cover the gaps introduced by §11.1–11.7.

#### Additions to `tests/core/test_config_loader.py`

- [x] P1 `test_load_env_config_has_version_key` — `"version"` key exists in env config dict
- [x] P1 `test_load_rl_config_has_version_key` — `"version"` key exists in rl config dict
- [x] P1 `test_load_rl_config_has_vis_delay_ms_key` — `"vis_delay_ms"` key exists in rl config
- [x] P2 `test_load_rl_config_vis_delay_ms_is_int` — `config["vis_delay_ms"]` is `int`
- [x] P2 `test_load_rl_config_vis_delay_ms_non_negative` — value is `>= 0`

#### Additions to `tests/sdk/test_training_loop.py`

- [x] P2 `test_training_loop_default_vis_delay_is_zero` — `loop.vis_delay == 0.0` after construction
- [x] P2 `test_training_loop_set_vis_delay_100ms_stores_01s` — `set_vis_delay(100)` → `loop.vis_delay == 0.1`
- [x] P2 `test_training_loop_set_vis_delay_zero_is_valid` — `set_vis_delay(0)` does not raise
- [x] P2 `test_training_loop_set_vis_delay_500ms_stores_05s` — `set_vis_delay(500)` → `loop.vis_delay == 0.5`

#### Additions to `tests/sdk/test_drone_sim_sdk.py`

- [x] P1 `test_sdk_load_default_grid_no_error` — `load_default_grid()` does not raise
- [x] P1 `test_sdk_load_default_grid_grid_has_one_start` — exactly one `CellType.START` after reset
- [x] P1 `test_sdk_load_default_grid_grid_has_one_target` — exactly one `CellType.TARGET` after reset
- [x] P1 `test_sdk_load_default_grid_matches_config_dimensions` — grid size matches `config/env.json`
- [x] P2 `test_sdk_load_default_grid_after_edit_restores_original` — restores layout after `update_grid` changes
- [x] P2 `test_sdk_set_vis_delay_delegates_to_loop` — calling `sdk.set_vis_delay(200)` changes `training_loop.vis_delay`

---

### 11.9 Phase 11 Completion Gate

- [x] P1 Run `uv run pytest` → all tests GREEN including new §11.8 additions
- [x] P1 Run `uv run ruff check .` → 0 errors
- [x] P1 Verify `config/env.json` has `"version"` key
- [x] P1 Verify `config/rl.json` has `"version"` and `"vis_delay_ms"` keys
- [x] P1 Confirm `TrainingLoop` has `set_vis_delay()` method
- [x] P1 Confirm `DroneSimSDK` has `load_default_grid()` and `set_vis_delay()` methods
- [x] P1 Confirm `GraphPanel` embeds `NavigationToolbar2Tk`
- [x] P1 Confirm `GridEditorMixin` performs BFS reachability check after every cell paint
- [x] P1 Confirm `App._poll_queue` throttles `GridCanvas.refresh()` by `vis_every_n`
- [x] P1 Confirm all source modules have module-level docstrings
- [x] P1 Run `uv run pytest --cov=src --cov-fail-under=85` → PASS

---

*This TODO tracks all tasks for the Drone Q-Learning Simulation project. Update status symbols as work progresses. Mark `[x]` only when the task's definition-of-done is fully satisfied.*
